# QinTopia PMS v1.2.2

发布日期：2026-09-09。保留浏览器 favicon，同时撤回应用页面内的图片 Logo 展示。

## 优化说明

- 登录页和主界面左侧导航恢复为文字品牌布局。
- 保留浏览器标签页 favicon 与 Apple Touch Icon，浏览器仍显示用户提供的 Logo。
- 清理不再使用的应用内图片 Logo 样式，避免页面增加无必要的视觉元素。

## 升级说明

- 无数据库迁移，无新增运行时配置，不改变订单、资金、库存、会员、权限或会话行为。
- 更新 Web 容器后刷新浏览器即可看到应用内 Logo 回退；浏览器标签页仍使用 Logo。

## 验证与已知问题

- 已通过 `npm run release:check`、`npm run typecheck`、`npm run build` 和 `git diff --check`。
- 已确认应用源代码仅在 `index.html` 中保留 favicon 与 Apple Touch Icon 引用，登录页和主界面不再渲染图片 Logo。
- 生产部署于 2026-09-09 15:31（北京时间）完成；提交 `f246a043367030005b983d514cb1d991dfe2481c`、容器健康检查、`/health/ready` 和 `/api/v1/version`（`1.2.2`）均通过。
- 线上 favicon 返回正常，生产构建未发现应用内图片 Logo 引用；上一版镜像已保留用于回退。

## 回退说明

- 本次为纯前端静态资源和布局调整，回退到 `v1.2.1` 可恢复应用页面内的图片 Logo。
- 无需回退数据库或业务数据。
