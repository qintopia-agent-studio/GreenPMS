## 改动说明

事件游标过期时，原错误 schema 将应返回的410序列化为500，导致 Agent OS 无法启动重建。本次为事件 feed 声明精确的 CURSOR_EXPIRED 响应，保留 rebuild_required=true。同时接受两个已有来源的支付 schema 精确指纹，使原迁移新安装能启动；未知结构和权限漂移继续拒绝。

复用现有 Webhook，补双程序合成联调工具、版本清单及失败/成功证据。另补既有退款字段的 OpenAPI 测试夹具和支付事件500响应声明，不改变支付业务。README 纠正 PostgreSQL 版本的概括。运行代码仅涉及两个 API 路由文件和一个 readiness 文件；无迁移、依赖、生产配置或 Agent OS 源码变更。

## 验证结果

已完成的固定版本证据：Agent OS v2 双程序联调19/19；A最终冻结版共享构建/迁移限定回归4/4，两者范围分别记录。事件专项23项、单元1135项、TypeScript和构建/release元数据检查通过。readiness专项1项通过；OpenAPI首轮19项通过、2项修正后定向通过，不算整组重跑。

本次版本收口重新核验两份清单314个文件、两个程序与10个PMS关键文件hash；原业务代码未改，不重置联调库。PR标题正文校验、格式检查器8项、文档链接/JSON/差异检查通过；与2026-09-11核对的main可无冲突合并。详情见 `待开发项/PMS-AgentOS-事件联合验收-20260911.md` 及 `待开发项/PMS-AgentOS-版本收口与PR准备-20260911.md`。远端PR尚未创建，GitHub必需检查仍待实际PR触发。

## 风险与回退

联调使用PG18；v1.3.2发布记录写生产PG18.4，当前目标库版本尚未现场复核。README/本地Compose旧默认PG16不代表当前事件指纹兼容，已有记录显示PG16不兼容；不能据此PR宣称PG16通过或直接升级数据库。发布前在实际目标同版本隔离环境补适用readiness/事件验收，核验迁移基线和受限角色。

本切片不新增迁移。发布/回退使用最新main的既有harness；直接回退须双方镜像及迁移兼容性满足same-migrations-only。需要停止事件投递时保留Outbox、水位、Inbox和在途回执，不删除队列。回退本修复会重现410变500及新安装拒绝启动，优先前向修复；不回退数据库。

生产网络/TLS、READ权限、密钥轮换、吞吐与24小时积压、灾备和完整真实欢迎仍待后续验收。本PR准备不授权推送、合并、发布Release、启用真实Worker或外部发送。
