---
title: QinTopia PMS 分步开发与人工验收计划
type: human-review-development-plan
status: in-progress
implementation_authorized: true
created: 2026-07-23
baseline_commit: 4fbf90daa47803ed4ac7ae3f4f46d256665d1e16
current_step: step_3_accepted
execution_model: one-step-one-human-acceptance
technical_companion: ./房态与订单运营流程分步开发计划.md
---

# QinTopia PMS 分步开发与人工验收计划

这是本轮开发的**人工审阅唯一入口**。它只回答三件事：每一步做什么、系统自动检查什么、工作人员如何在真实页面验收。字段、事务、接口和测试细节见[工程实施清单](./房态与订单运营流程分步开发计划.md)。

## 先确认执行方式

- 第 2 步已于 2026-07-24 通过人工验收；用户于 2026-07-25 明确回复“第三步通过”，第 3 步阶段 5、6、7 全部验收通过。
- 用户免除阶段 6 单独人工检测；阶段 6、阶段 7 与手机端已完成合并验收。阶段 8 和第 4 步仍未开始。
- 每一步包含若干小的技术检查点；每个检查点完成后我都会提供可访问网址、演示数据和点击顺序，然后停止开发，避免一次交付过大。
- 你确认该步骤的全部检查点并回复“本步通过”后，才进入下一步；未通过时只修当前步骤。
- 人工验收使用真实 Web、API 和 PostgreSQL，不用 mock 或静态演示。
- 技术协议继续保留在后台，但工作人员页面只使用中文业务语言。
- 冲突、并发、陈旧数据、网络中断、损坏恢复记录和受限 Token 等场景全部由开发侧提前准备；不会要求你造测试数据、打开开发者工具或直接操作数据库。你只需要按给出的页面步骤操作并判断业务结果，零写入和事务回滚由自动化测试证明。

## 开发顺序

| 步骤 | 交付主题 | 技术清单 | 人工验收重点 |
|---:|---|---:|---|
| 1 | 自动报价、普通订单和免费入住 | 阶段 1-3 | 能否顺畅完成第一次开单 |
| 2 | 会员列表、会员订单、企微收款和权益 | 阶段 4 | 能否建会员、卖会员卡、看到真实余额并用于住宿 |
| 3 | 房态昵称、维修锁房、整段选择和订单操作区 | 阶段 5-7 | 房态是否一眼看懂，点击后能否找到正确订单与动作 |
| 4 | 入住、退房、续住、缩短、换房、取消、未到和资金关联 | 阶段 8-13 | 第一次开单以后的运营动作是否完整、可审计 |
| 5 | 90 天连续时间轴和超长住宿 | 阶段 14 | 能否跨可见窗口拖选，90 天外仍能正确建单 |
| 6 | 外围智能体、安全恢复和发布验收 | 阶段 15 | API、并发、恢复、备份及完整旅程是否达到可用标准 |

---

## 第 1 步：自动报价、普通订单和免费入住

### 开发做什么

- 房态拖选，或在右侧填写房源、入住日和退房日后，自动显示晚数和住宿金额。
- 删除“应用选区”“清除选区”“获取服务端报价”和人工选择“临住/周住/月住”等步骤。
- 普通订单填写入住人姓名、昵称、订单来源；系统按日期自动选择已确认价格档位。
- 点击“创建订单”后只出现一页中文订单概述，再点击一次“确认创建订单”。
- 正常价格不要求原因；只有勾选“调整最终总价”时才填写最终总价和调价原因。
- 免费入住复用同一套入住人资料，必选“义工”或“接待”并填写具体原因；不显示订单来源、会员和收费字段，金额始终为 0。
- 房号统一显示为 `D01-D05`、`E01-E03`；内部库存 ID 不变，房数、床数和基础库存数不变。

### 系统自动检查

- 6 夜使用临住价格，7 夜进入 7 夜档；14、30 夜边界、跨月和最终整元四舍五入均按已确认政策。
- 104 房间从 `2026-07-26` 住到 `2026-08-05` 为 10 夜，金额为 `¥1,086`。
- 房间与床位双向互斥；已占日期、无效日期、陈旧报价和重复提交都不能产生半笔订单。
- 房态表、房源下拉、右侧库存信息和办理区统一显示“楼栋 + 房号 + 房型”，不重复房号；例如 `3栋 302 单人间（公卫）`。
- 四个正常订单来源 code 正确；企业微信订单来源不保存渠道订单号。
- 免费入住缺少入住人、昵称、类型或原因时零写入；不得消耗会员权益。
- 房号改名后仍闭合为 44 间实体房、91 张实体床、77 个基础销售库存。

### 你如何人工验收

1. 进入房态，确认 D/E 楼房号显示为 `D01-D05`、`E01-E03`。
2. 选择 104，入住 `2026-07-26`、退房 `2026-08-05`；不再点击中间按钮，直接看到“10 晚、¥1,086”。
3. 填写入住人姓名、昵称和渠道，点击“创建订单”；确认只有一页中文订单概述和一次正式确认。
4. 分别测试“不改价”和“调整最终总价”，确认只有改价时才出现必填原因。
5. 另选空房创建免费入住，填写同样的入住人资料、义工/接待和具体原因；确认没有渠道和收费字段。
6. 在已占日期尝试建单，确认页面说清楚哪个房源、哪段日期冲突，并且没有第二张订单。

### 本步通过标准

普通订单和免费入住都能从选房到创建完成；页面没有多余中间步骤、英文协议名或内部 ID。你回复“第 1 步通过”后进入第 2 步。

---

## 第 2 步：会员列表、会员订单、企微收款和权益

### 开发做什么

- 提供统一会员列表，可按姓名、身份证号、手机号和微信号查找。
- 手工创建会员档案：姓名、身份证号、手机号、微信号；身份证号是唯一业务键，内部仍使用不可变会员 ID。
- 会员间夜不作为可覆盖的档案数字。每笔权益来自一张会员订单或一笔不可变权益调整，余额由系统账本计算。
- 提供三种会员产品：

| 会员产品 | 默认成交价 | 发放权益 | 只能用于 |
|---|---:|---:|---|
| 公卫单人间 | ¥1,620 | 30 间夜 | 公卫单人间 |
| 独卫单人间 | ¥2,160 | 30 间夜 | 独卫单人间 |
| 公卫四人间 | ¥936 | 30 床夜 | 四人间公卫的单床，不含整房组合 |

- 默认价可由工作人员改成实际成交价，但必须填写调价原因，并保留标价、成交价、差额和历史。
- 一张会员订单可登记多笔企微收款，每笔填写独立企微交易单号；工作人员点击“生效”前至少要有一笔收款。
- 收款合计与成交价不一致时只提示差额，不阻止工作人员明确点击“生效”。
- 有效期从点击“生效”开始计算一年，并发放 30 个对应类型的间夜或床夜。
- 录错交易单号、金额或间夜时，不覆盖历史：页面提供“更正”，后台冲销原事实并追加正确事实，完整保留审计链。
- 普通住宿默认不显示会员搜索。勾选“本次住宿使用会员权益”后，才显示会员查找和覆盖摘要。
- 会员住宿不填写订单来源渠道；无论权益全额、部分还是零余额覆盖，渠道和渠道订单号都不显示且不保存。非会员普通住宿仍必须选择渠道。
- 会员住宿按钮、核对、成功和恢复流程只显示中文业务语言，不出现 Preview、Receipt、Command 或内部 ID。
- 选择会员后，主要居住人的昵称和姓名预填会员姓名，联系电话和证件号码预填会员手机号与身份证号；这些只是本次住宿快照的初始值，操作人可逐项修改。
- 权益不足时先覆盖可用日期，剩余日期逐晚使用该房型的 1 夜临住价格；后续新增权益可覆盖尚未覆盖日期。

### 系统自动检查

- 同一身份证号不能创建两个会员；手机号和微信号不做唯一限制。
- 三种会员权益只能用于上表对应房型，四人间会员只覆盖具体床位。
- 未生效订单不发放权益；没有企微收款不能生效；重复点击生效不能重复发放 30 夜。
- 多笔收款、收款更正、权益调整和余额都可以追到原事实；同一事实不能被静默覆盖。
- 预订冻结、入住核销、入住前取消/未到释放、缩短释放未核销日期均保持余额守恒；并发预订不能超额冻结。
- 会员覆盖日期不能重复，权益不能折算成现金折扣，现金也不能伪装成权益。

### 你如何人工验收

| 检查点 | 本次只验收 | 通过口令 |
|---|---|---|
| 2A | 会员列表、建档、身份证去重和四种资料搜索 | `2A 通过` |
| 2B | 三种会员订单、默认/成交价、多笔企微收款、更正和显式生效 | `2B 通过` |
| 2C | 账本余额、房型匹配、部分覆盖、冻结与入住核销 | `2C 通过` |

1. 打开会员列表，新建一位会员，再用身份证号搜索并打开其档案。
2. 创建公卫单人间会员订单，确认默认价为 ¥1,620；改一次成交价，确认必须填写原因。
3. 登记两笔企微收款和两个不同交易单号；在合计不等于成交价时确认只出现提示。
4. 点击“生效”，确认生效日从本次点击开始、到期日为一年后、余额为 30 间夜。
5. 更正一笔收款和一次间夜，确认原记录仍可见，余额来自新追加的更正事实。
6. 分别用 `D01`、`B01`、`101A` 验证三种会员产品；尝试不匹配房型时必须明确拒绝且不扣权益。
7. 创建会员住宿，确认未勾选时看不到会员搜索；勾选并选择会员后，四项主要居住人资料自动预填且都能修改。确认渠道消失、按钮显示“核对并创建订单”，核对页采用修改后的快照且没有英文协议名或内部 ID。
8. 办理入住，确认已冻结权益在入住成功时核销；普通订单和免费入住不受影响。

### 本步通过标准

工作人员能从会员列表完成“建档 → 会员订单 → 多笔企微收款 → 生效 → 查看余额 → 会员住宿”，且所有历史可追溯。你回复“第 2 步通过”后进入第 3 步。

---

## 第 3 步：房态昵称、维修锁房、整段选择和订单操作区

### 开发做什么

- 多人间父房逐日格直接显示当天所有住客昵称和 `已占/总床数`，不再显示“名字 +1”。
- 大床房、双人间及其他整房订单按房间权威入住容量登记全部住宿人；房态横排显示全部昵称与人数，完整姓名、电话和证件资料只在订单详情展示。
- 已预订显示“已预订”，办理入住后显示“在住”；正常占用不再显示“阻断”“Claim”等机器语言。
- 房态日历默认只在逐日格显示居住人昵称、人数/比例和必要状态，不重复显示带文字的住宿横条；点击住宿后用整段连续高亮表达范围，订单号和订单入口只保留在右侧“选中对象”订单上下文栏。维修/锁房等 Block 横条继续显示。
- 点击一个住客的任意住宿日期，按真实订单与 Stay 选中整段连续住宿，而不是只选一天。
- 整房订单的已占房间格提供该订单入口；分床父房聚合格不提供含糊入口，展开后每个具体床位格提供该床对应的准确订单入口。
- 续住和换房仍属于同一订单容器，但内部保留清楚的原始预订、续住、缩短和换房分段边界。
- 右侧由“报价草稿”切换为该订单的基础信息、当前住宿安排、变更记录和服务端允许的操作。
- 右侧支持人工更正本订单居住人快照中的昵称、姓名、联系电话和证件号码；必须记录原因、修改前后值、操作人和时间。会员住宿也只更正订单快照，不自动同步会员主档。
- 房态以主表为首要工作区；常见笔记本分辨率不再过早进入挤压主表的双栏布局。右侧订单操作区在空间不足时改为可打开、可关闭且保留焦点的抽屉，日期工具栏、筛选区和页面间距进入紧凑模式，不要求工作人员缩放浏览器才能操作。
- 日期格保持固定、可读的单日宽度，点击订单、打开或关闭上下文都不改变格子尺寸。`自动` 模式按房态表实际可用宽度在 7 至 21 天之间适配，典型 1440px 笔记本约显示 10 天，1920px 大屏自动显示更多；同时提供 `7 / 14 / 21` 手动档位。
- 维修锁房继续独立显示和释放，不计入住人、不收费；“内部占用”在经营边界确认前不增加入口或新类型。

### 系统自动检查

- 1/2/3/4 个床位占用时，昵称顺序、比例和当天实际入住人完全一致；同名住客也不能被合并。
- 整房订单人数不得超过独立入住容量，床位订单容量固定为 1；Preview 后容量变化必须失败关闭，换房也不得把多人订单移入低容量房间。
- 房态 DTO、Tooltip、移动摘要和无障碍名称只包含住宿人 ID、昵称与人数，不泄露姓名、电话或证件号；订单详情仍能查看全部住宿人完整快照。
- 相邻但不同的订单、同昵称订单和存在日期间隔的新住宿不能误合并。
- 默认房态不存在重复的住宿文字横条；逐日格、悬停信息和无障碍名称均不泄露 `order_...` 订单号。点击后整段高亮仍按底层稳定关联定位，右侧订单上下文显示正确订单号；维修/锁房等 Block 横条不受影响。
- 整房格只进入唯一整房订单；分床父房聚合格不猜测订单，展开后的每个床位格都进入自身稳定引用对应的订单。
- 居住人资料更正是追加式审计操作，房态和订单详情同步刷新；无原因、越权、直接覆盖历史或隐式修改会员主档均被拒绝。
- 无缝换房可以跨两个房源行共同选中；续住前后有一个订单外框，也有可审计分界。
- 只读账号看不到写操作；所有动作由服务端状态和权限决定。
- 鼠标、键盘、触控和屏幕阅读器得到同一订单上下文。
- 在 `1280x720`、`1366x768`、`1440x800` 三种典型笔记本视口中，房态主表首屏至少可见 4 行，页面无横向溢出，日期、筛选、选区和订单操作均可到达；打开和关闭订单抽屉不丢失选区、滚动位置或键盘焦点。
- 自动日期窗口按房态表实际宽度计算且限制在 7 至 21 天；1440px 下约显示 10 个 94px 日期格，1920px 下显示更多。点击订单及上下文开关前后日期格宽度不变，手动 `7 / 14 / 21` 与恢复 `自动` 均可预测。
- 维修锁房继续与房间/床位库存互斥，释放后恢复可售且不丢历史。

### 你如何人工验收

1. 在四人间安排 2、3、4 位住客，确认父房格按两人一行横排显示全部昵称和 `2/4、3/4、4/4`；同名与“历史未记录”不被合并。
2. 查看大床房和整间双人房的两人订单，确认房态显示两个短昵称与 `2人`，订单详情显示两位住宿人的完整资料。
3. 点击王羽住宿中的任一天，确认默认没有重复的住宿文字横条，点击后整段高亮，右侧显示王羽的订单号和订单上下文而不是报价表单。
4. 查看一笔续住订单，确认原日期和新增日期属于同一订单，但续住分界仍清楚可查。
5. 查看一笔无缝换房订单，确认原房源和新房源的住宿分段共同高亮。
6. 确认格子、悬停、右侧和无障碍名称均不出现“阻断、Claim、Receipt”等机器语言。
7. 创建并释放一次维修锁房，确认它不被显示成住客或免费入住；页面中没有尚未确认的“内部占用”入口。
8. 在典型笔记本视口中确认 `自动` 模式约显示 10 天、单日格清晰可读；点击订单及打开/关闭上下文前后格子不变大，抽屉关闭后仍回到原选区和焦点。再在更宽屏幕确认自动显示更多日期，并切换 `7 / 14 / 21` 后恢复 `自动`。
9. 展开一个分床多人间，分别点击两个床位订单，确认各自进入准确订单；再更正一笔订单的昵称或联系方式，确认房态、订单详情和审计记录一致更新，会员主档不被隐式修改。

### 本步通过标准

工作人员可以从房态一眼识别住客和占用率，并从任意住宿格进入正确订单和正确操作。你回复“第 3 步通过”后进入第 4 步。

---

## 第 4 步：履约、订单变更、资金关联和住宿转会员

本步按以下小项依次交付；每个小项都使用独立订单验收，避免前一个终态污染后一个测试。每完成一个小项都会停下供你实际操作，通过后才继续本步的下一个小项。

### 开发做什么

1. **入住与退房：** 住宿状态按“已预订 → 在住 → 已退房”变化，退房后另行生成“待清洁”任务；会员在入住成功时核销已冻结权益。待清洁是否阻断可售在用户确认前不硬编码。
2. **续住：** 以最初入住日至新退房日的累计总晚数重新选档，继续使用成交时锁定的价格版本；已入住会员的新增覆盖在续住确认时立即核销。
3. **缩短：** 按缩短后的完整总晚数重新报价；只在已记录净收款高于新金额时提示可供人工退款处理的正差额。
4. **换房：** 连续住宿不被打断；用累计总晚数选择一次档位，各住宿分段精确计价，最终总价只舍入一次。
5. **取消与未到：** 释放未入住库存和仍被冻结的权益，不恢复已经核销的权益；逾期仍未入住的预订必须出现在工作人员可见的异常队列。
6. **住宿资金：** 每笔收款和退款独立录入交易单号，退款引用原收款；金额在页面按“元”输入和显示，不要求工作人员输入“分”。
7. **住宿收款转会员：** 系统以不可变会员 ID 关联目标会员，并以身份证号核实和主要居住人是同一自然人；允许把一笔或多笔完整的原住宿收款转入匹配房型的会员订单，再登记带新企微交易号的差额收款。转入、会员订单生效、30 夜发放、本次住宿专用核销、剩余权益和住宿重价必须一次成功或全部失败。

住宿收款转会员只允许**整笔转入**，不拆分单笔收款，不复制原交易号，不允许同一笔收款重复转入，也不建设通用跨订单分摊。

### 系统自动检查

- 所有动作追加变更、计价修订和审计，不覆盖原订单、旧房间、旧日期或旧价格。
- 续住、缩短、换房始终使用原锁定价格版本；旧手工调价不自动继承。
- 陈旧确认、并发占房、重复请求和任一步失败均零部分写入。
- 收款、退款、冲销和转入不会重复计入；交易号不被解释成到账、结清或自动核销。
- 转会员只允许身份证号相同的住客和匹配房型；有退款、冲销、超额或已转入的收款必须先处理清楚。
- 示例必须通过：公卫单人间住 7 夜、原收 ¥590，整笔转入默认价 ¥1,620 的会员订单，再收 ¥1,030；发放 30 间夜，以专用转换核销事实立即扣除本次 7 夜，剩余 23 夜，住宿当前金额和现金余量变为 0，原现金计价与收款历史仍保留。会员有效期不回溯，普通会员覆盖规则也不允许借此处理其他历史住宿。

### 你如何人工验收

| 检查点 | 本次只验收 | 通过口令 |
|---|---|---|
| 4.1 | 入住、退房和独立清洁任务 | `4.1 通过` |
| 4.2 | 续住及累计晚数重价 | `4.2 通过` |
| 4.3 | 缩短、重价和正退款差额提示 | `4.3 通过` |
| 4.4 | 换房、连续分段和最终一次舍入 | `4.4 通过` |
| 4.5 | 取消、未到、库存和未核销权益释放 | `4.5 通过` |
| 4.6 | 多笔收退款、交易号、按元录入和变更关联 | `4.6 通过` |
| 4.7 | 住宿收款整笔转会员及防重复/原子回滚 | `4.7 通过` |

1. `4.1`：分别用普通、会员、免费订单办理入住和退房，核对住宿状态、独立清洁任务和会员核销；本检查点不预判待清洁是否影响可售。
2. `4.2`：对独立订单续住并跨过价格档位，核对累计晚数、新金额、原因和旧记录。
3. `4.3`：对另一订单缩短，核对重新落档金额；只有已记录净收款高于新金额时才提示正退款差额。
4. `4.4`：完成一次同价和一次跨价换房，确认连续分段、统一档位和最终总价只舍入一次。
5. `4.5`：用两张独立订单分别取消和标记未到，确认库存与未核销权益正确释放，逾期未到订单不会从工作台消失。
6. `4.6`：录入多笔收款和一笔引用原收款的退款，确认页面按元输入，所有交易号和所属变更都能查到。
7. `4.7`：按上面的 ¥590 + ¥1,030 示例完成一次住宿转会员；确认原交易没有复制、会员剩余 23 夜、住宿当前金额和现金余量为 0，两边都能追溯原现金历史和专用转换核销。随后再次尝试转入同一笔收款，确认明确拒绝且没有新增事实。

### 本步通过标准

第一次开单以后的常用操作都能从订单上下文找到，资金与权益不重复、不丢失，原始历史始终可查。你回复“第 4 步通过”后进入第 5 步。

---

## 第 5 步：90 天连续时间轴和超长住宿

### 开发做什么

- 房态加载连续 90 天，桌面约显示 14 天，可用横向滚动条浏览完整范围。
- 拖选接近左右边缘时自动滚动，并持续扩展同一个房源选区。
- 超过 90 天的住宿在右侧输入完整入住、退房日期，不要求网格绘制全部日期。
- 报价和最终创建都校验完整住宿区间，第 91 天以后的冲突也必须发现。
- 保持多人间全部昵称、订单整段选择、典型笔记本紧凑布局、键盘、移动端和 200% 缩放可用。

### 系统自动检查

- 90 天数据量下滚动、拖选和刷新达到既定性能预算，不丢选区或错列。
- 90 天外冲突由 PostgreSQL 权威拒绝，返回具体房源和日期且零写入。
- `1280x720`、`1366x768`、`1440x800`、平板、375/320px、200% 缩放、键盘和屏幕阅读器均无重叠或不可达操作，90 天时间轴不得把第 3 步已通过的主表优先和订单抽屉布局重新改回固定挤压双栏。

### 你如何人工验收

1. 从 `2026-07-26` 拖到至少 `2026-08-20`，确认跨出初始 14 天窗口后仍是一个选区。
2. 检查左右边缘自动滚动、反向拖选和滚动条定位。
3. 在右侧输入 `2026-07-26` 至 `2026-11-20`，确认显示 117 夜并能正常报价。
4. 打开我预置的“第 91 夜已有占用”验收场景并提交超长住宿，确认系统指出具体冲突而不是只检查当前画面。
5. 在典型笔记本、平板、手机和 200% 缩放下检查昵称、日期和操作没有重叠，并确认笔记本仍保持第 3 步通过的主表优先布局。

### 本步通过标准

90 天内可以连续拖选，超长住宿可以通过右侧建单，二者都校验完整区间。你回复“第 5 步通过”后进入第 6 步。

---

## 第 6 步：外围智能体、安全恢复和发布验收

### 开发做什么

- 保持 Web 与外围智能体 API 使用同一套业务命令、权限、事务和审计。
- 验证受限 Token 的查询、写入收窄、过期、撤销、轮换、幂等重放和中断恢复。
- 把损坏或未知的本地恢复状态改成工作人员看得懂、能安全处理的流程；不能简单清缓存后盲目重试。
- 导航统一使用“今日履约”等工作人员语言；确认按钮按真实风险区分普通主操作和危险操作，不把所有写命令都显示成红色。
- 生产环境不展示演示密码；Token 管理入口按权限显示；关键页面自动刷新时保留编辑内容、选区和焦点。
- 完成并发、事务回滚、OpenAPI、备份恢复、readiness、无障碍、响应式和完整浏览器旅程。

### 系统自动检查

- 单元、领域、真实 PostgreSQL 集成、OpenAPI contract、计价事实、构建和浏览器 E2E 全部通过。
- 房床并发互斥、权益并发冻结、陈旧确认、重复请求和网络中断恢复均有真实数据库测试。
- 备份可以恢复到新数据库并通过 readiness；核心实现没有 mock、占位逻辑或未关闭关键 TODO。
- 外围投影不能写回核心事实，也不能成为 PMS 的运行依赖。
- 响应式截图和几何回归覆盖 `1280x720`、`1366x768`、`1440x800`、平板、375/320px 与 200% 缩放；典型笔记本上房态主表、订单抽屉和关键操作满足第 3 步已通过的可见性与焦点要求。

### 你如何人工验收

1. 完整走通：查房态 → 自动报价 → 会员覆盖 → 建单 → 分次收款 → 入住 → 续住 → 换房 → 缩短 → 引用原收款退款 → 退房。
2. 走通免费入住和会员订单转入两条独立旅程。
3. 查看我用预置受限 Token 运行的验收结果，核对查询、高风险操作、幂等重放和中断后结果查询四种结果都使用同一业务规则。
4. 在我预置好网络中断和本地恢复信息损坏的验收浏览器中继续操作，确认页面给出可执行的中文处理步骤，不会盲目重复业务事实。
5. 检查今日异常、典型笔记本视口、移动端、键盘、320px、200% 缩放和主要错误提示。

### 本步通过标准

全部自动化门禁通过，你完成完整人工旅程，系统提供最终运行地址、启动命令、账号、测试结果、截图、备份恢复结果和剩余非阻断风险后，才能把本计划标记为完成。

## 实施前仍需确认的少量经营事实

这些问题只阻断对应小项，不阻断前面的可确定开发：

1. 同一会员存在多份可覆盖同一日期、同一类型的有效权益时，先消耗哪一份。
2. 免费入住与“内部占用”的精确经营边界；未确认前不实现内部占用，不影响免费入住和维修锁房。
3. 旧住宿订单没有身份证号时，住宿转会员应如何由工作人员证明“同一住客”；未确认前不能仅凭姓名或昵称自动匹配。
4. 未到的营业日期边界，以及已经入住后提前结束住宿应使用哪一个业务动作。
5. 普通住宿收退款是否继续要求填写“支付方式”，以及这个字段的真实业务含义；不能把订单来源“企业微信”自动当作普通住宿的支付方式。
6. 待清洁是否影响可售、只读访问令牌可以看哪些个人信息、是否把交班汇总纳入 MVP。

会员合同退费当前不进入开发范围，因为合同尚未拟定。计划和页面只表达“当前版本不处理退费”，不写成法律意义上的“不可退款”。

## 状态记录

| 步骤/检查点 | 状态 | 人工验收结果 |
|---:|---|---|
| 1 | 进行中 | 阶段 1 已于 2026-07-23 通过人工验收；阶段 2 等待在新对话中启动；阶段 3 未开始 |
| 2A | 已通过 | 2026-07-24 人工验收通过：四字段建档/展示/搜索、身份证规范化去重、重复联系方式及中文核对/成功界面符合预期 |
| 2B | 已通过 | 2026-07-24 用户完成人工验收：三产品、调价、多笔企微收款、更正、差额生效、一年有效期和一次性 30 夜发放均符合预期 |
| 2C | 已通过 | 2026-07-24 用户明确回复“第 2 步通过”；余额、会员住宿、无渠道、中文核对及可编辑居住人快照符合预期 |
| 3（阶段 5） | 已通过 | 2026-07-24 用户完成复验并回复“检查完了，继续”；整房多人、全部昵称、横排布局、默认无住宿文字横条与 Block 横条保留均通过 |
| 3（阶段 6） | 已通过 | 2026-07-25 随第 3 步合并验收通过：维修锁房边界符合预期；内部占用继续保持待决且无新写入口 |
| 3（阶段 7） | 已通过 | 2026-07-25 用户明确回复“第三步通过”；连续 Stay、准确订单上下文、资料更正、桌面/手机响应式及 21 天档位均通过；阶段 8 未开始 |
| 4.1 | 未开始 | - |
| 4.2 | 未开始 | - |
| 4.3 | 未开始 | - |
| 4.4 | 未开始 | - |
| 4.5 | 未开始 | - |
| 4.6 | 未开始 | - |
| 4.7 | 未开始 | - |
| 5 | 未开始 | - |
| 6 | 未开始 | - |
