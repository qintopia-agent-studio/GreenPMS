---
title: QinTopia PMS 房态与订单运营流程分步开发计划
type: pending-development-plan
status: in-progress
implementation_authorized: true
created: 2026-07-23
baseline_commit: 4fbf90daa47803ed4ac7ae3f4f46d256665d1e16
current_stage: stage_15_in_progress
execution_model: tiered-flow-with-batched-human-acceptance
scope: 房态选区、自动计价、订单创建、免费入住、会员档案与会员订单、多人间全昵称、订单变更、收退款与住宿转会员、30 天时间轴
companions:
  - ./QinTopia-PMS-分步开发与人工验收计划.md
  - ../docs/implementation/spec-qintopia-pms-core-operations-mvp.md
  - ../docs/implementation/room-status-ui-development-goal.md
  - ../docs/implementation/room-status-grid-implementation-spec.md
  - ../docs/implementation/spec-guest-nickname-bed-occupancy.md
  - ./房态30天连续时间轴与超长住宿开发规格.md
  - ./QinTopia-PMS-第4步-4.2-实施规格.md
  - ./QinTopia-PMS-第4步-4.3-实施规格.md
  - ./QinTopia-PMS-第4步-4.4-实施规格.md
  - ../design-system/qintopia-pms/MASTER.md
---

# 房态与订单运营流程分步开发计划

## 0. 执行方式

用户审阅、启动与逐步验收请统一从[《QinTopia PMS 分步开发与人工验收计划》](./QinTopia-PMS-分步开发与人工验收计划.md)进入；本文件不作为要求用户逐条阅读的入口。

本计划记录用户在现有 RoomStatusGrid 上确认和仍待核定的新一轮运营工作流改造。当前仅供完整讨论与审阅，不构成实施授权；只有用户明确说“开始实施第 1 步”后，才按人工入口中的 6 个步骤执行。本文件的 15 个技术阶段是步骤内的小检查点：每个技术阶段仍单独交付和验收，但只有同一人工步骤内的全部阶段通过后，才把人工入口推进到下一步。

每个阶段均按以下闭环执行：

1. 实施该阶段的 Web、API、领域、数据库及测试改动，不只修改 UI。
2. 运行该阶段的聚焦自动化测试，并重新启动可试用实例。
3. 向用户提供实际访问 URL、测试结果和逐步点击说明。
4. 停止进入下一阶段，等待用户回复“通过”或提出修改意见。
5. 若用户不通过，只修复当前阶段并重新交付；不得提前夹带后续阶段。
6. 用户通过后固定当前行为，再开始下一阶段。

每次人工验收使用独立、可识别且无冲突的订单或日期夹具。取消、未到、退房等互斥终态必须使用不同订单，不能让前一项操作污染后一项结果。交付验收前还要确认浏览器控制台无新增错误、关键网络请求状态正确，并说明是否保留了本次验收数据。

一个阶段只有同时满足以下条件才算“可提交验收”：

- 页面使用真实 API 和 PostgreSQL，不使用 mock、占位数据或静态演示结果；
- Web 与 API 仍使用相同领域规则、授权、事务、幂等和持久结果；
- 聚焦的 unit/domain、integration、contract 和 browser E2E 已通过；
- 可试用实例的 health/readiness 正常；
- 工作人员可按本文件的人工验收步骤独立完成操作；
- 不在工作人员主流程中暴露实现协议或内部代码。

## 1. 文档权威与覆盖关系

1. `QinTopia-PMS-分步开发与人工验收计划.md` 是用户逐步审阅、启动和验收的唯一入口；本文件是它的工程实施清单。两者冲突时，先暂停对应切片并修正文档，不允许开发人员自行选择解释。
2. `spec-qintopia-pms-core-operations-mvp.md` 继续负责库存、计价、会员、资金、事务和审计不变量；本计划只在明确列出的用户最新决定上精确覆盖旧表述，不把展示偏好扩展成其他领域变化。核心规格中尚未同步的旧会员有效期、退费和参考产品表述，应在实施对应阶段时一并修订，不能继续作为运行规则。
3. 本计划负责最新确认的工作人员操作流程、字段渐进显示、中文文案和逐阶段人工验收顺序。
4. `房态90天连续时间轴与超长住宿开发规格.md` 是尚未纳入版本控制的旧阶段 14 草案；其中 90 天窗口、约 14 天可见窗口和边缘自动滚动决策已被 2026-07-27 用户批准的连续 30 天、整页纵向滚动方案覆盖。开始阶段 14 前必须在保留该既有文件的前提下形成并提交新版 30 天规格；未完成权威规格同步不得编码。
5. `design-system/qintopia-pms/MASTER.md` 继续负责视觉、响应式和无障碍基线。
6. 所有 companion（包括 90 天规格和旧 RoomStatusGrid 目标）中的 Preview、Confirm、Receipt、Quote ID、policy ID 等协议仍是后端/API 合同，但其工作人员可见文案和交互步骤全部被本计划覆盖：日常操作页面不得直接展示这些名称和内部 ID。
7. 外围智能体 API、审计查询和故障恢复仍保留稳定 Preview、Confirm、Receipt、command、correlation 和幂等语义；隐藏技术词不等于删除协议。
8. 核心规格中“所有新 Order 必须选择四个渠道之一”的旧表述，自本计划起收窄为“所有新建非会员正常住宿订单”。免费入住和会员住宿是明确例外，渠道和渠道订单号都必须保存为 `null`；会员权益不足而产生未覆盖金额也不改变住宿来源。绕过 Web 提交会员渠道必须由服务端与数据库拒绝且零业务写入。这不改变历史空值原样兼容规则，也不新增第五个渠道。

## 2. 已确认的人员与业务身份

必须始终区分三个主体：

- **登录工作人员：** 当前登录 PMS、执行操作和留下审计记录的人。
- **主要居住人：** 实际入住的住客，保存为订单成交时的不可变快照。
- **权益所属会员：** 会员权益的所有人；只有勾选使用会员权益时才选择，可能与主要居住人相同，也可能不同。

系统目前不是会员自助系统。页面不得把登录工作人员描述成会员本人，也不得要求工作人员选择或理解会员合同 ID。

## 3. 业务类型与字段边界

| 业务类型 | 是否有入住人 | 入住人信息 | 渠道 | 金额与会员 | 最终事实 |
|---|---:|---|---|---|---|
| 非会员正常住宿 | 是 | 姓名、昵称必填；联系电话、证件号沿用现有住客字段与校验 | 必选游牧岛、携程、美团或企业微信；外部渠道订单号必填，企业微信渠道订单号必须为空 | 自动保留政策基础价；外部渠道必填本单渠道应结金额，企业微信直订默认政策价且可说明原因后人工调价 | Order + Stay + Claim + initial Pricing Revision |
| 会员住宿 | 是 | 与正常住宿复用同一个住客组件、同一组字段和同一套校验 | `bookingChannelCode=null`，`channelOrderReference=null` | 权益可全额、部分或零余额覆盖；未覆盖部分按规则计价 | Order + Stay + Claim + Entitlement Coverage |
| 免费入住 | 是 | 与正常住宿复用同一个住客组件、同一组字段和同一套校验 | `bookingChannelCode=null`，`channelOrderReference=null` | 金额始终为 0；不得使用会员权益 | Order + Stay + Claim |
| 内部占用（边界待核定） | 待确认 | 候选方案为不显示、不保存入住人 | 不适用 | 候选方案为不计价、不收款、不使用会员权益 | 候选方案为 INTERNAL_USE Block + Claim |
| 维修锁房 | 否 | 不显示、不保存入住人 | 不适用 | 不计价、不收款、不使用会员权益 | MAINTENANCE Block + Claim |

免费入住还必须填写：

- 免费入住类型 `freeStayCategoryCode`：`VOLUNTEER`（义工）或 `RECEPTION`（接待）；
- 本次免费入住的具体原因；
- 房源、入住日期和退房日期。

数据库列 `free_stay_category_code` 对历史免费订单允许为 `null`，Query/DTO 原样返回，Web 只派生显示“历史未记录”；所有新建免费入住 Command 必须提供两个合法 code 之一。迁移不得把未知历史行虚构回填成义工或接待。暂不增加“其他”免费类型，不虚构批准人或审批流。免费入住的入住、续住、缩短、换房、取消和退房与普通住宿使用相同履约模型；每次变更追加 amendment、pricing revision 和审计，金额保持 0。

### 3.1 渠道订单金额边界

- `YOUMUDAO`、`CTRIP`、`MEITUAN` 是外部渠道；渠道是该住宿订单的客户。
- 页面使用“本单渠道应结金额”，表示渠道针对这一张订单应结给物业的金额。该值就是订单首个计价修订的合同金额，不另建重复的渠道结算金额字段。
- 本单渠道应结金额不是住客向平台支付的金额，不是平台多单合并打款，也不是银行实际到账。
- 外部渠道订单的渠道订单号和本单渠道应结金额都必填；页面不得把政策价静默当作工作人员已经核对的渠道金额。
- 政策基础金额与锁定政策版本永久保留。`abs(渠道应结金额 - 政策基础金额) / 政策基础金额 <= 15%` 属于正常渠道价格范围，不要求自由文本说明；正好 15% 也不要求。
- 差异超过 15% 时显示并必填“渠道价格差异说明”。服务端用整数金额判断 `abs(target - policyBase) * 100 > policyBase * 15`，Web/API/数据库事务路径不得分叉。
- 企业微信 `WECOM` 作为直订来源，渠道订单号必须为 `null`。默认采用政策价；只要工作人员主动偏离政策价，就必须填写人工调价原因。
- 创建后更正任何已保存价格均要求更正原因并追加 revision，不覆盖原始事实；后续 revision 不继承旧渠道价格或旧手工调整。
- PMS 不新增平台结算单、合并打款、银行到账/流水、跨订单分摊或“待结算”状态。外部财务对账智能体通过 PMS API 取得渠道、渠道订单号、政策基础金额、订单合同金额和追加式变更事实，再结合系统外财务资料完成总账核对。

## 4. 工作人员可见语言

主流程使用以下中文业务语言：

- `正在计算住宿金额`
- `住宿金额`
- `计价依据`
- `本次住宿使用会员权益`
- `会员权益覆盖晚数`
- `未覆盖晚数`
- `权益未覆盖金额`
- `订单概述`
- `确认创建订单`
- `订单已创建`
- `房态或价格已经变化，请重新核对`
- `正在查询刚才的操作结果`

下列内容不得直接出现在工作人员的报价、订单概述、成功页或常规订单详情主区域：

- `Preview`、`Confirm`、`Receipt`、`Command`；
- `Quote ID`、`Receipt ID`、`Command ID`、correlation ID、幂等键；
- pricing policy code、policy version ID、Lot ID、Coverage ID；
- `coverageSet`、`cashRemainder`、`currentContractAmount` 等字段名；
- “获取服务端报价”“现金计价行”“资源引用”等开发或协议语言。

内部稳定引用继续保存在 API、数据库、审计和故障恢复中。获权的技术审计入口可以单独查看，但不能成为完成日常订单的必经页面。

### 4.1 房态状态与机器事实

- 工作人员房态统一使用“已预订”和“在住”：订单创建且尚未办理入住时显示“已预订”；办理入住成功后立即显示“在住”。“已入住”只描述一次已经发生的动作，不作为持续状态；“入住中”不再作为另一套同义状态。
- 办理退房后，订单详情显示“已退房”；房态不再把该订单表达为当前在住，并按库存事实释放后续日期。根据 2026-07-26 人工验收决定，当前版本不生成、投影、展示或完成清洁任务；既有 `PENDING` 清洁记录不进入房态或订单界面。历史日期仍可追溯当日住宿事实，但不得让工作人员误解为住客当前仍在住。
- 正常订单或免费入住已经占用库存时，服务端仍必须保留 `blocking`、`conflicts`、Claim 和完整冲突引用，用于可售判断、并发互斥和确认时重新校验；工作人员格子、Tooltip、屏读名称及常规详情不显示“阻断”“阻断库存”“N 个阻断”“库存 Claim”等机器语言。
- “已预订”或“在住”本身已经说明该日期不能重复出售，不再叠加红色阻断数量。只有出现真正需要工作人员处理的异常，或工作人员尝试在已占日期创建业务时，才用中文业务句子说明具体对象和日期，例如“101A 在 7 月 23 日至 28 日已有住宿，不能重复安排”，而不是暴露冲突对象代码。

### 4.2 订单日期调整与履约动作矩阵

订单是不可删除的经营容器，Stay 是同一笔连续住宿。工作人员调整已经预订的日期时，不取消、不删除原订单，也不重新下单；同一事务追加 amendment、不可变住宿安排版本和完整 pricing revision，迁移库存 Claim，并只调整仍为 HELD 的会员覆盖。原日期、原房源、原金额和原渠道事实继续保留为历史。

“尚未履行”只表示未来服务日期尚未发生，不表示存在“尚未核销的房间”。库存 Claim、订单合同金额和会员权益是三类不同事实：会员订单在 CHECK_IN 时已把本次住宿全部 HELD 覆盖转为 CONSUMED，入住后缩短不会恢复已核销权益，但仍必须缩短住宿安排、释放未来库存并重算订单金额。

| 当前状态与营业日期 | 可进入的业务流程 | 当前阶段未完成时的处理 |
|---|---|---|
| `RESERVED`，营业日早于计划到店日 | 未入住改期或取消；不得入住、退房或标记未到 | 改期在阶段 9 完成前保持原订单不变；非法履约零写入 |
| `RESERVED`，营业日等于计划到店日 | 普通入住；入住前仍可按未入住改期/取消处理 | 4.1 只验收普通入住；未到的最早办理时点留给阶段 12 |
| `RESERVED`，营业日晚于计划到店日 | 进入“逾期未到”队列，由补办入住、改期、取消或标记未到的已确认规则决定 | 阶段 12 关闭日期 Gate 前不得自动改状态；普通入口不得只按 `RESERVED` 状态全部开放 |
| `CHECKED_IN`，营业日早于计划退房日 | 可在住续住；可缩短到营业日之后并继续在住；仅当入住日早于营业日时，缩短到营业日当天属于提前退房 | 阶段 10 完成前普通 CHECK_OUT 对提前退房失败关闭；入住当天提前退房继续拒绝 |
| `CHECKED_IN`，营业日等于计划退房日 | 普通退房，或在退房前续住 | 4.1 只在该边界验收普通退房 |
| `CHECKED_IN`，营业日晚于计划退房日 | 统一按工作人员迟录原计划退房处理；如需继续住宿，由工作人员主动选择续住 | 订单生命周期仍为在住，但计划占用不自动延期；只进入非阻断异常任务，当前及未来房格保持按真实 Claim 可售；退房以原计划退房日为生效日、当前营业日为办理日，日期、金额和计价修订不变 |
| `CHECKED_OUT/CANCELLED/NO_SHOW` | 只读历史、获准的资料/资金更正；不得再改住宿日期 | 非法日期动作零写入 |

所有日期范围继续使用物业时区半开区间 `[arrivalDate, departureDate)`，并满足 `departureDate > arrivalDate`。阶段 10 对已入住缩短使用严格三分支：`newDepartureDate > businessDate` 为缩短后继续在住；`newDepartureDate = businessDate` 且 `arrivalDate < businessDate` 为提前退房原子结算；`newDepartureDate < businessDate` 为追溯改写并拒绝。若 `businessDate = arrivalDate`，当前版本不办理提前退房、不生成零夜计价；未实际使用房间的撤销入住归阶段 12。

每次日期变化均用原锁定政策版本对完整有效住宿安排重新落档，旧渠道价和旧人工调整不自动继承。外部渠道订单重新确认本次渠道应结金额并执行 15% 说明规则；WECOM 默认政策价、主动偏价才要求人工调价原因；免费保持 0；会员重新计算覆盖与现金余量。页面同时展示订单新金额、已登记净收款和差额，只提供补收/退款参考，不自动收款或退款。

## 5. 目标主流程

工作人员完成一笔正常订单时，应只有以下可感知步骤：

1. 在房态网格拖选房源和日期，或在右侧输入房源、入住日期和退房日期。
2. 系统自动检查完整区间，并显示总晚数、住宿金额和简明计价依据。
3. 工作人员填写主要居住人、渠道等当前订单需要的信息；只有勾选会员权益后才出现会员搜索。
4. 点击“创建订单”，进入不写业务事实的订单概述。
5. 核对住客、房源、日期、晚数、会员覆盖、政策基础金额和订单合同金额；外部渠道显示“本单渠道应结金额”。
6. 外部渠道价格差异不超过 15% 时不要求原因，超过 15% 时填写“渠道价格差异说明”；企业微信直订主动偏离政策价时填写人工调价原因。
7. 点击一次“确认创建订单”。服务端重新校验并原子创建订单。
8. 成功后只反馈订单号、住客、房源、日期、金额和下一步可执行操作；前置纠偏 U1 完成后不再保留独立成功回执页。

后端仍在上述流程中执行高风险预检、显式确认、重新校验、幂等和持久结果，但工作人员不需要理解协议名称或复制任何代码。

### 5.1 操作意图、变更原因与调价原因

三种“原因”不得混为一个自由文本字段：

- **创建普通订单的操作意图：** 工作人员无需填写原因。Web/API 仍在命令协议中保存与命令类型绑定的稳定业务原因码，例如 `CREATE_STANDARD_ORDER`，`note` 为空；该码只说明正在创建普通订单，不得伪造成工作人员陈述。登录主体、时间和 correlation 继续由审计事实记录。
- **住宿变更原因：** 续住、缩短、换房、取消、未到以及免费住宿变更按各阶段要求由工作人员填写，保存为该 amendment 的业务原因。
- **渠道价格差异说明：** 外部渠道订单金额是渠道合同事实，不属于人工改价。只有本单渠道应结金额与政策基础金额的绝对差异率超过 15% 时才显示并必填；15% 及以内不要求说明。
- **人工调价原因：** 企业微信直订主动偏离政策基础价时显示并必填，专门解释本次人工决定，不能用命令原因码代替。
- **价格更正原因：** 订单创建后更正任何已保存金额时始终必填，保留原值、新值、操作人和时间。

外围智能体仍必须按 API 合同提交获准的稳定 reason code；Token 不得因此绕过 reason、Preview、显式 Confirm 或审计。服务端必须验证 reason code 与 command type 匹配，不能接受任意客户端文本冒充结构化操作意图。

CREATE_ORDER 必须在同一个原子命令中保存政策基础金额、目标合同金额和差额：外部渠道目标金额来自本单渠道应结金额，企业微信直订默认采用政策价或由工作人员显式人工调价。禁止先按政策价创建再调用 REPRICE_ORDER。续住、缩短和换房仍按各阶段政策重算；任何后续 revision 都不继承旧渠道价格或旧手工调整。

## 6. 当前已知缺口

实施前以当前工作树复核，已知至少包括：

1. Web 将新付费订单固定为 `TRANSIENT`，导致 7 夜以上被服务端正确拒绝；104 房间 `2026-07-26` 至 `2026-08-05` 的 10 夜订单目前无法报价。
2. 前端把部分确定性 4xx 校验失败误判为网络结果未知，错误进入恢复锁定状态。
3. 住宿类型仍由工作人员选择，且存在“获取服务端报价”按钮。
4. 房态侧栏存在“应用选区”和“清除选区”两个多余中间动作。
5. 报价区域展示逐日英文现金行、内部政策代码、Quote/Receipt ID 和字段名。
6. 创建订单存在多层 Preview/Confirm/Receipt 技术页面，工作人员难以判断真正需要确认几次。
7. `/api/v1/orders/:id` 的 amendment payload 序列化与 schema 不一致，真实订单详情可能返回 500，间接隐藏续住、缩短、换房、入住和退房入口。
8. 会员搜索与会员合同默认暴露，普通订单也显示 `coverageSet 0 晚`，容易被误解为住宿晚数为 0。
9. 多人间父房格虽然已取得完整逐床住客列表，但当前只显示“首个昵称 +N”，其余昵称只有悬停后可见，不符合首页直接显示每位住宿人的最新要求。
10. 90 夜查询上限已存在，但连续 90 天时间轴、约 14 天可见窗口和拖选边缘自动滚动尚未完成。
11. 点击已占日期当前只形成单日选区，无法按稳定订单/Stay 引用选中完整连续住宿；右侧仍停留在日期草稿与报价上下文，不能直接查看该订单的业务概述和当前可执行操作。
12. 正常已预订订单的每个日期格仍额外显示“1 个阻断”，Tooltip、屏读名称和右侧详情还暴露“阻断库存、阻断冲突、库存 Claim”等机器协议；该信息只是库存不可重复出售的内部依据，不是工作人员需要处理的异常。
13. 会员当前只有零散档案/合同能力，缺少工作人员可用的统一会员列表、会员订单、多笔企微收款、生效动作、权益余额与不可变交易/核销记录闭环。
14. 当前 catalog 仍把会员产品标为参考数据、把有效期起点写成付款日、把退费写成不可退款；最新确认规则是工作人员点击“生效”后起算一年，退费在合同拟定前不进入当前版本，不能继续沿用旧运行语义。
15. D/E 楼仍显示 `D-GEN-*`、`E-GEN-*`；应改为 `D01-D05`、`E01-E03`，继续保留 `PMS_GENERATED` provenance 和内部不可变库存 ID。
16. 资金表单目前按最小货币单位录入，而人工调价按元录入，存在 100 倍误录风险；工作人员端应统一按人民币元输入和显示，API/数据库继续使用精确 minor units。
17. 今日履约页面遗漏“到店日已过、仍为已预订且尚未超过离店日”的订单；本地恢复记录损坏时会阻断当前浏览器会话的写操作，但没有工作人员可执行的安全恢复路径。这两项是发布前门禁，不阻断阶段 1 的报价改造。
18. 房态双栏当前按浏览器 viewport 过早启用，没有扣除 `210px` 左导航和页面内边距；常见笔记本上右侧上下文会把房态主表挤成狭小窗口。页面也缺少针对短屏的紧凑密度和典型 `1280x720`、`1366x768`、`1440x800` 几何回归。该缺口归入人工第 3 步的阶段 7 修复，不提前夹带到正在执行的第 2 步。

## 7. 分阶段实施与人工验收

### 阶段 1：选房后自动显示住宿金额

**目标：** 工作人员选择房源和日期后，不再选择住宿类型、不再应用选区、不再主动请求报价，系统直接显示权威住宿金额。

**实施范围：**

- 网格拖选结束后立即同步右侧房源和半开日期；侧栏修改房源或日期后立即同步当前网格可见交集。
- 桌面横向拖选锁定起始房间或床位行，只按鼠标横坐标更新日期；纵向漂移到相邻行不得切换行、停止日期更新或显示相邻行 hover，重复落在同一日期不得触发重复刷新。
- 删除主流程中的“应用选区”和“清除选区”。新选区直接替换旧草稿；关闭办理区域只放弃未提交草稿，不写业务事实。
- 删除付费住宿的“临住、周住、月住、自定义周期、固定期限、滚动续期”等人工选择。
- 付费 Quote/CreateOrder 以完整连续住宿晚数自动落入 rev561 的 1/7/14/30 夜价格档位。
- API 中付费住宿类型不得覆盖日期推导结果；若为了 `/api/v1` 稳定性保留旧输入，只能作为兼容字段并拒绝与服务端推导不一致的值。
- 报价自动触发需防抖、取消过期请求，并正确区分确定性校验失败与真正网络未知。
- 页面只展示日期、晚数、简明计价依据和总住宿金额，不展示逐日现金行或内部 ID。
- 房态表、房源下拉、右侧库存信息、办理住宿区和移动任务统一使用楼栋 / 房号 / 房型展示函数，已含房号的库存 `name` 不得再与 `code` 重复拼接。
- 已占日期或整房/子床互斥冲突继续由服务端和 PostgreSQL 权威拒绝且零写入；Web 只显示具体房源、重叠日期和不能操作的业务原因，不显示 `conflict`、Claim、blocking fact、对象代码或“阻断”数量。
- 将 PMS 生成的 D/E 楼运营房号统一为 `D01-D05`、`E01-E03`；只改稳定运营 code 和所有引用夹具，不改内部不可变库存 ID，并继续标记 `codeProvenance=PMS_GENERATED`，不得伪称为源表原始房号。

**自动化门禁：**

- 6 夜使用 P1，7 夜切入 P7 档；14、30 夜边界继续使用既有金标规则。
- 104 整房 10 夜按 7 夜档计算并最终 half-up 为 `¥1,086`。
- 房床冲突、无效日期和政策未生效返回确定性中文错误，不进入网络未知恢复状态。
- 使用真实 PostgreSQL 预置已占订单后，API/DTO 仍返回可审计的稳定冲突与 Claim 引用；Browser E2E 尝试在重叠日期报价或创建时保持零写入，并断言工作人员只看到“房源 + 重叠日期 + 不能重复安排”的中文业务句，不出现 `conflict`、Claim、blocking 或内部 ID。
- 快速连续修改日期时，迟到响应不能覆盖最新日期的金额。
- 拖选指针直接漂移到相邻行时，选区仍只沿起始行按横坐标扩展，起始行位置和高度保持不变；正常松键、`Escape`、pointer cancel 和捕获丢失都必须清理拖选状态。
- 拖选期间只更新网格内本地预览，不改变右侧日期或发送 Quote；松键后只提交一次最终选区和一次 Quote。逐帧取样的右侧滚动容器 `scrollTop`、`clientHeight`、`scrollHeight` 和归一化位置必须全程不变。
- 302 在房源下拉和右侧库存信息中必须显示为 `3栋 302 单人间（公卫）`，不得出现 `302 · 302`；同一规则覆盖所有房间和床位。
- Web、API contract 和 PostgreSQL Quote 结果一致。
- 房号改名后的 catalog、种子、Query、价格事实和 E2E 不再出现 `D-GEN-*`/`E-GEN-*`；44 房、91 床、77 个基础销售库存及各楼栋床数交叉校验保持不变。

**用户人工验收：**

1. 登录并进入房态，确认 D/E 楼显示为 `D01-D05`、`E01-E03`，且房间和床位总数没有变化。
2. 选择 104 房间，入住 `2026-07-26`，退房 `2026-08-05`。
3. 不点击“应用选区”或“获取服务端报价”，直接看到“10 晚”和 `¥1,086`。
4. 将退房日改成 6 夜和 7 夜边界，确认金额会自动更新，且 7 夜允许出现比 6 夜更低的已确认跳档结果。
5. 在网格重新拖选另一房源，确认右侧草稿立即替换且没有产生订单。
6. 在同一房间行横向拉选时，故意让鼠标轻微上下偏移到相邻行，确认选区仍锁定在起始房间行，方格不再上下跳动，表格不抖动。
7. 先将右侧办理区滚动条拉到底部，再在 102 同一行从 `2026-07-26` 缓慢拖到 `2026-07-31`，每个日期稍停一下；确认拖动中右侧滚动条不上下抖动，松键后一次更新为 `2026-07-26` 至 `2026-08-01`、6 晚和 `¥1,392`。
8. 选择 302，确认房态表、房源下拉、右侧“库存单元”和住宿金额区都显示 `3栋 302 单人间（公卫）`，不再显示 `302 · 302`；再抽查一个其他楼栋房间和一个床位。
9. 选择一段已经被订单占用的房源日期，确认系统明确说明“哪个房源、哪段日期已有住宿、不能重复安排”，且页面不出现“阻断、Claim、conflict”或任何内部对象代码；随后确认数据库没有产生第二笔订单或部分写入。

**停点：** 用户确认选区和自动计价体验通过后，才能进入阶段 2。

### 阶段 2：正常订单概述、渠道应结金额与一次确认

**目标：** 用工作人员能理解的订单概述代替技术 Preview/Confirm/Receipt 页面。

**实施范围：**

- 正常订单表单只显示本阶段需要的主要居住人、房源日期、渠道和金额。
- 主要居住人姓名、昵称必填；联系电话和证件号沿用现有字段与领域校验。
- 渠道持久化稳定 code，界面显示中文；YOUMUDAO/CTRIP/MEITUAN 的渠道订单号和“本单渠道应结金额”必填，WECOM 隐藏或禁用渠道订单号并强制保存 `null`。
- “创建订单”只打开订单概述，不产生 Order/Stay/Claim。
- 概述页后台取得与当前表单完全绑定的服务端预检结果，显示住客、房源、日期、晚数、渠道、政策基础金额、订单合同金额和差异；外部渠道的订单合同金额在页面命名为“本单渠道应结金额”。
- 外部渠道金额与政策价的差异不属于人工改价：差异率不超过 15% 不显示原因，超过 15% 才显示并必填“渠道价格差异说明”。WECOM 直订默认政策价，只有主动偏离时显示并必填人工调价原因。
- 正常创建不要求工作人员填写自由文本原因；命令保存与 `CREATE_ORDER` 绑定的稳定操作意图 reason code 和空 note，不能伪造工作人员说明。
- 目标合同金额与初始订单在同一原子 CREATE_ORDER 中形成首个完整 pricing revision，不能先建政策价订单再二次修改；不新增重复的渠道应结金额持久字段。
- 概述只提供“返回修改”和“确认创建订单”；正式业务确认只有一次。
- 本次阶段 2 返工沿用当前已实现的共享命令外壳，只新增必要业务字段和校验；独立成功页、自动预检和机器信息收敛留给前置纠偏 U1/U2，不在本切片提前修改。
- 保持 amendment payload 查询 DTO/schema 一致，确保创建后的“查看订单”入口能够打开刚创建的真实订单；完整详情布局和动作可发现性仍由既有阶段负责。

**自动化门禁：**

- 四个合法渠道、三个外部渠道订单号和本单渠道应结金额必填、WECOM 强制空渠道订单号通过 domain/integration/contract/E2E。
- 未填写姓名、昵称或渠道时零业务写入。
- 政策价、外部渠道应结金额和 WECOM 人工目标价分别产生正确且不可变的首个 revision；CREATE_ORDER 重放不产生第二条 REPRICE revision。
- 外部渠道绝对差异率 15% 及以内不要求说明，超过 15% 说明必填；WECOM 任何人工偏价原因必填。目标金额必须为非负人民币整数元，百分比门槛使用整数比较。
- 服务端只接受与命令类型匹配的稳定操作意图 reason code；普通创建的空 note 与人工调价的必填原因分别通过 contract test。
- 重复确认或网络恢复不重复产生订单、Stay、Claim 或 revision。
- 新创建的订单详情 Query 不再因 amendment payload schema 返回 500。

**用户人工验收：**

1. 在当前 21 天查询窗口内使用五个不同房间的一晚草稿填写住客姓名、昵称和渠道；每笔直接点击对应房态格形成完整入住/退房区间，不依赖跨阶段的 90 天时间轴。
2. 点击“创建订单”，确认只进入一页中文“订单概述”。
3. 使用外部渠道分别录入政策价 85%、84% 和 116% 的本单渠道应结金额，确认 15% 边界无需说明，超过 15% 才要求“渠道价格差异说明”。
4. 从创建成功反馈打开刚创建的订单，确认可以正常进入详情而不是内部错误。
5. 再建一笔 WECOM 直订订单：政策价无需原因，主动偏离政策价时必须填写人工调价原因；创建后更正金额始终需要更正原因。
6. 故意返回修改日期，确认旧概述不会被直接提交，必须重新核对更新后的金额。

**停点：** 用户确认正常订单创建流程和文案通过后，才能进入阶段 3。

### 阶段 3：免费入住

**目标：** 免费入住是一笔有真实住客、正常履约但金额为 0 的住宿订单。

**实施范围：**

- “创建免费入住”进入独立业务模式，但复用正常住宿同一个主要居住人表单组件、字段、校验和不可变快照。
- 必选“义工”或“接待”，并填写具体免费原因。
- 不显示会员复选框、会员搜索、订单来源和渠道订单号。
- 数据库、Command、Preview effect、Confirm、Receipt/Query 和 OpenAPI 保存 nullable 渠道以及稳定免费类型和原因。
- currentContractAmount 始终为 0，不创建会员 coverage，不产生电费或其他现金计价项。
- 免费订单概述和成功页使用与正常订单一致的人类语言，只把金额明确显示为“免费”。

**自动化门禁：**

- 缺失姓名、昵称、免费类型或原因时拒绝且零写入。
- 免费订单的渠道、渠道订单号和会员合同均为 `null`。
- 重放不重复创建订单；房床互斥与普通订单完全相同。
- Query/详情能追溯入住人、免费类型和原因。
- migration、Command schema、effect、Confirm、Receipt、Query DTO、OpenAPI 和 Web 对 `freeStayCategoryCode` 使用同一枚举；历史 `null` 不被伪造回填。

**用户人工验收：**

1. 在空闲房源选择日期并点击“创建免费入住”。
2. 确认需要填写与普通住客相同的姓名、昵称等入住人信息。
3. 选择“义工”或“接待”并填写具体原因。
4. 确认页面不出现渠道、会员和收费字段，订单概述显示金额为免费。
5. 创建后在房态上看到有住客昵称的免费住宿，而不是内部占用。

**停点：** 用户确认免费入住资料和房态表达通过后，才能进入阶段 4。

### 阶段 4：会员档案、会员订单与会员入住

**目标：** 工作人员能从统一会员列表完成建档、出售会员产品、多笔企微收款、生效、余额查看和会员住宿；余额来自不可变 Lot/Ledger，不是可以直接覆盖的档案字段。

**实施范围：**

- 建立统一会员列表和详情。会员档案支持手工录入姓名、身份证号、手机号和微信号；身份证号是唯一业务键，内部关联始终使用不可变 `memberId`，手机号和微信号不承担唯一性。
- 支持按姓名、身份证号、手机号和微信号搜索。会员详情显示当前 ROOM_NIGHT/BED_NIGHT 余额、会员订单、权益 Lot/Ledger、冻结、核销、释放和显式调整记录。
- 飞书入住申请只作为外部申请引用：以 `provider=FEISHU_BASE + base/table + external_record_id` 唯一标识一次申请并关联 `memberId`；同一人多个 record_id 可以关联同一会员。Base 不能直接发放、冻结或核销权益，PMS 正确性不能依赖回写成功，也不得复制现有 workflow 的认证令牌。
- 会员间夜只能由已生效会员订单或显式权益调整事实产生。若工作人员需要更正余额，Web 以“更正后剩余间夜数 + 原因”为业务输入，服务端计算调整量并追加 Ledger 事实；不得覆盖档案余额或删除旧核销记录。
- 将源表中的三个 `REFERENCE_ONLY` 会员产品固化为版本化、可执行且可种子的会员产品 catalog，不建设通用会员产品编辑器：

| 产品 | 默认价 | 发放 | 允许房型/销售单位 |
|---|---:|---:|---|
| 公卫单人间会员 | `¥1,620` | 30 ROOM_NIGHT | `shared_bath_single` 独立房间：`D01`、`D02`、201、205、301-304 |
| 独卫单人间会员 | `¥2,160` | 30 ROOM_NIGHT | `private_bath_single` 独立房间：B01、B02、C01-C04、E02 |
| 公卫四人间会员 | `¥936` | 30 BED_NIGHT | `shared_bath_quad` 单床：101、102、103、105、107、108、109、202、203、206 的 A/B/C/D；不含整房组合 |

- 会员订单保存稳定 `membershipOrderId`、产品、标价、成交价、调价差额、调价原因、状态、生效时间和完整审计。Web/API 输入的是工作人员指定的成交价；未偏离默认价时不要求原因，偏离时原因必填。成交价必须是非负人民币整数元。
- 一张会员订单允许多笔独立企微收款，每笔保存自身外部交易单号。会员订单上下文本身已经限定这是企微收款，不向工作人员提供其他支付方式选项，也不得因此新增通用 `transactionChannelCode`；这条事实只适用于会员订单，不得推断普通住宿资金的 `method`。
- 会员订单在工作人员点击“生效”前至少要有一笔有效企微收款；收款合计与成交价不一致时只显示差额，不阻断显式生效。有效期从服务端记录的生效时点起算一年，并原子发放 30 个对应类型权益。
- 会员订单收款的交易号或金额录错时，提供“更正”命令：冲销原资金事实并追加正确事实；权益间夜更正同样追加调整事实。工作人员可看到当前有效结果和完整历史，但任何已存在的资金、核销或审计事实不可原地编辑或删除。
- 当前版本不实现会员合同退费，也不创建退费公式、退款命令或“已用权益按日报价结算”逻辑。由于会员合同尚未拟定，Web/规格只表达“当前版本不处理会员退费”，不得将其扩大为法律意义的不可退款承诺；旧 `NON_REFUNDABLE_MEMBERSHIP` 运行表述必须退役并保留被最新决定覆盖的 provenance。
- 普通住宿默认不显示会员搜索、会员合同和覆盖区域；提供未默认勾选的“本次住宿使用会员权益”。勾选后显示统一会员选择器，工作人员选择会员档案而不是合同 ID。
- 勾选会员权益后，订单来源渠道和渠道订单号立即隐藏并清空；全额、部分和零余额覆盖都按会员住宿处理，CREATE_ORDER 不提交渠道。非会员普通住宿仍必须选择四个合法渠道之一。
- 会员住宿的主按钮为“核对并创建订单”；核对、成功、持久化恢复和订单详情只显示中文业务语义。订单详情显示“住宿来源：会员权益”，不得显示渠道字段、Preview、Receipt、Command、幂等键或内部 ID。
- 选择会员后，以会员姓名同时预填主要居住人昵称和姓名，以会员手机号、身份证号预填联系电话和证件号码；微信号不映射为入住人昵称。这四项是可编辑的本次住宿快照，报价刷新不得覆盖人工修改，CREATE_ORDER 必须保存提交时的快照值。
- 服务端按 `memberId` 查找日期有效、余额可用且 ROOM_NIGHT/BED_NIGHT 与库存单位匹配的 Lot/Ledger，按具体住宿日期形成 coverageSet 并冻结；同一日期不得重复覆盖，并发请求不得超额冻结。
- 权益不足不拒绝订单。先覆盖可用日期，剩余日期统一按锁定政策版本的 P1 逐晚计算现金余量，最终总价只舍入一次；后续新增有效会员订单可以覆盖尚未覆盖日期并重新计算现金余量。
- UI 只显示总住宿晚数、覆盖晚数、未覆盖晚数和权益未覆盖金额。普通订单完全隐藏“覆盖 0 晚”；会员模式确实无余额时显示“覆盖 0 晚、未覆盖 N 晚”，不能让人误解为本次住宿是 0 晚。
- 预订确认冻结；CHECK_IN 成功时将 HELD 转为 CONSUMED；入住前取消/未到释放；入住前缩短释放减少日期；已核销权益不被普通取消、缩短或退房隐式恢复。已入住后续住的新增覆盖在续住确认事务中立即核销。

**自动化门禁：**

- 身份证号唯一、会员搜索、Feishu record_id 多申请关联同一 `memberId` 及外部引用去重在领域、PostgreSQL 和 contract 测试通过。
- 三种会员产品的默认价、稳定 code、权益类型、精确房型映射和整房排除均有金标；不匹配房型失败关闭且零权益、零库存写入。
- 未收款不能生效；收款差额只提示；生效幂等重放只发放一个 30 夜 Lot；有效期精确从服务端生效时点起算一年。
- 多笔企微收款各自保存非空交易号，但不施加未经确认的全局唯一约束；收款更正、权益更正均保留原事实并追加冲销/调整，Query、Receipt 和审计链可完整追溯。
- 全额覆盖、部分覆盖、0 余额、续充后刷新、预订冻结、CHECK_IN 核销、取消/缩短释放和已核销不恢复均通过真实 PostgreSQL 验收。
- 同一住宿日期不得重复覆盖；并发会员订单或住宿订单不得重复生效、重复发放或超额冻结。
- 未勾选会员时不得读取、冻结或核销任何会员权益；API 可复算 coverageSet，Web 中文摘要与其精确一致。
- 会员住宿省略渠道可成功创建；强行提交任一渠道必须在预检、确认和数据库层失败且零业务写入；非会员普通住宿省略渠道继续失败。
- 桌面与手机 E2E 覆盖会员住宿表单、中文核对/成功、恢复语义和订单详情，确认无渠道字段、英文协议名、内部 ID 或横向溢出。
- 桌面与手机 E2E 选择会员后核对四项预填值，再逐项修改并断言 CREATE_ORDER 使用修改后的主要居住人快照。

**阶段 4 的局部业务 gate：**

同一会员若同时存在多个可覆盖同一日期、同一权益类型的有效 Lot，优先消耗哪一份额度会影响到期和余额事实。当前用户尚未确认分配顺序，不得自行选择“最早到期优先”、创建时间优先或任意数据库顺序。会员档案、会员订单、企微收款、生效和单一 Lot 旅程可先实施；开始多 Lot 自动 coverage 前必须确认唯一顺序并加入领域金标。

**用户人工验收：**

1. 打开会员列表，创建会员并按姓名、身份证号、手机号和微信号分别查找；重复身份证号必须明确拒绝。
2. 分别创建三种会员订单，核对默认价、30 夜权益和允许房型；改变成交价时确认必须填写原因。
3. 在一张订单登记两笔不同企微交易号的收款；合计与成交价不等时只看到差额提示，没有自动改价或自动结清。
4. 尝试无收款生效应被拒绝；录入至少一笔后点击“生效”，确认有效期从本次点击起算一年且只发放一次 30 夜。
5. 更正一笔交易号/金额和一次“更正后剩余间夜数”，确认旧记录、冲销/调整、新记录和当前余额都能查到。
6. 打开普通住宿，确认默认没有会员搜索且非会员模式要求渠道；勾选后选择会员，确认渠道字段消失，四项主要居住人资料自动预填且可以分别修改，再查看覆盖摘要。取消勾选后渠道恢复且不残留会员事实。
7. 使用 `D01`、B01、`101A` 验证三个会员产品；尝试不匹配房型和四人间整房组合时明确拒绝且余额不变。
8. 点击“核对并创建订单”，确认核对、成功页和订单详情没有渠道、英文协议名或内部 ID，订单详情显示“住宿来源：会员权益”；再入住并确认预订时冻结、入住时核销。

**停点：** 用户确认会员列表、会员订单、多笔企微收款、生效、余额及会员住宿通过后，才能进入阶段 5。

### 阶段 5：多人间与整房首页显示全部住客昵称

**目标：** 多人间有多个床位被住宿订单占用时，父房逐日格直接显示每一个住客昵称；大床房、双人间等整房订单登记并显示全部住宿人，不再把第二个人以后压缩成“+N”或遗漏。

**实施范围：**

- 复用服务端 `bedOccupancies[].occupants` 的逐床权威结果；先验证 DTO 完整性，现有 DTO 已满足时不做无意义的后端改造，缺字段时才进行 additive contracts/API/OpenAPI 扩展。
- 为整房库存建立独立 `occupancy_capacity`，不以实体床数推测入住容量；床位容量固定为 1，未知房型失败关闭。大床房、双人间及其他整房订单可按容量登记有序同行人，主要住宿人不可删除，同名不去重。
- `CREATE_ORDER` 的 Preview 冻结住宿人稳定 ID、顺序和容量，Confirm 重新校验；订单创建后名单不可直接追加，换房不得把多人订单移入低容量房间。历史订单只回填一位已有主要住宿人，不虚构同行人。
- 会员选择只预填和关联主要住宿人；同行人姓名、昵称必填，电话和证件号可选。完整资料只进入获权订单详情，房态投影仅返回住宿人 ID、昵称和人数。
- 父房逐日格按 `inventoryUnitCode` 的稳定床位顺序显示每个有权查看的 `primaryOccupantLabel`。
- `1/4、2/4、3/4、4/4` 比例继续显示，所有昵称与比例必须同时可见；禁止“王羽 +1”“首个昵称 +N”、去重、隐藏后续昵称或仅在 Tooltip 中补全。
- 两个床位即使昵称相同也必须保留两项，不能因为文本相同而合并。历史缺失昵称按每个床位显示“历史未记录”，不得伪造昵称。
- 昵称优先两人一行横排并以顿号分隔；空间不足时智能换行，父房行才按内容统一增高。同一行所有日期列保持对齐，Hover、Tooltip 和状态变化不得再次改变尺寸或覆盖比例、日期及相邻格。
- Tooltip 和键盘详情继续显示床号、昵称、来源与日期，作为补充信息而不是完整昵称的唯一入口。
- 正常住宿和免费入住均计入昵称列表；维修、清洁和其他无居住人来源既不增加比例分子，也不得出现在住客昵称列表中。
- 桌面和平板房态格直接可见全部昵称；移动任务式界面的房间摘要或紧邻详情也必须直接列出全部有权昵称，不得恢复成 `+N`。
- 正常或免费住宿的日期格只显示昵称、占用比例和“已预订/在住”等业务状态，不显示“阻断”“N 个阻断”或冲突数量；Tooltip、title 和 accessible name 同样使用工作人员语言，不能把隐藏的技术词转移给屏幕阅读器。
- 默认房态只在逐日格显示昵称、人数/比例和必要业务状态，不重复渲染带文字的住宿横条；维修/锁房等 Block 横条继续显示。订单号只在阶段 7 的右侧订单上下文中显示，底层稳定订单引用继续用于点击定位和整段选择。

**自动化门禁：**

- Web state/component tests 覆盖 1、2、3、4 张床分别占用，并断言所有昵称按床号稳定顺序可见。
- PostgreSQL 集成测试覆盖整房双人、床位超容量、Preview 后容量变化、多人换入低容量房、创建后追加住宿人、并发超容量和历史迁移；失败场景均为零业务写入。
- Contract 与 E2E 验证两人整房的订单详情包含完整快照，而房态、Tooltip、移动摘要和无障碍名称不包含姓名、电话或证件号。
- E2E 明确断言父房格不存在 `+1`、`+2`、`+3` 折叠文案，且 `2/4、3/4、4/4` 与对应全部昵称同时出现。
- 同名住客、历史空昵称、正常与免费混合占用及维修排除均有回归测试。
- 鼠标悬停、键盘聚焦、屏读 accessible name 与可见昵称一致，获权 redaction 不泄露不可见住客。
- 正常已预订与在住日期格、Tooltip 和屏读名称均不存在“阻断”“阻断库存”“Claim”或冲突数量；底层 DTO 仍保留对应事实且可售判断不变。
- E2E 断言整房和父房聚合格默认没有重复住宿文字横条，逐日格、Tooltip、屏读名称和移动摘要均不包含 `order_` 订单号；维修/锁房等 Block 横条仍存在，隐藏显示不得删除 DTO 中的稳定订单引用。
- 1440、1024、768、375、320 CSS px 和 200% zoom 下无昵称遮挡、比例覆盖、日期错位或页面级横向溢出。
- 后续 90 天虚拟化必须保留本阶段的完整昵称与可变行高语义，不能为了性能重新折叠成 `+N`。

**用户人工验收：**

1. 打开一个四人间，在同一天安排两位不同昵称的住客。
2. 在首页父房格直接看到两个完整昵称和 `2/4`，确认不再出现“第一个名字 +1”。
3. 继续占用第三、第四张床，分别确认三个、四个昵称与 `3/4、4/4` 同时可见。
4. 检查跨多个日期时，每一天只显示当天实际占用床位对应的全部昵称。
5. 使用两个相同昵称占用不同床位，确认两项都保留；再检查历史未记录昵称的明确提示。
6. 加入维修来源，确认比例和住客昵称列表不会把它算作住宿人。
7. 悬停和键盘聚焦仍可查看床号与来源，但不使用悬停时也已经能看到全部昵称。
8. 检查整房和多人间父房格，确认昵称、人数/比例和必要状态可见，默认没有重复的住宿文字横条；维修/锁房横条仍显示。点击后的整段高亮和订单入口留待阶段 7 验收。
9. 检查王羽的已预订日期格、悬停提示和屏读名称，确认不再出现“1 个阻断”“阻断库存”或 Claim 等技术词，同时该床位仍不可被重复预订。
10. 查看大床房和整间双人房的两人订单，确认房态显示两个短昵称与 `2人`，订单详情显示两位住宿人的完整资料；房态不显示姓名、电话或证件号。

**停点：** 用户确认多人间每格的全部昵称、比例和布局通过后，才能进入阶段 6。

### 阶段 6：维修锁房与内部占用决策（内部占用暂不实施）

**目标：** 保持已经确认的维修锁房能力；用户尚未决定免费入住与内部占用的精确分类条件，因此内部占用只记录候选边界，不在确认前实施，也不阻断后续核心订单阶段。

**实施范围：**

- “维修锁房”保持独立类型和原因，通过服务端高风险命令创建 typed Block/Claim，不创建 Order、Stay、pricing revision 或资金事实。
- 维修释放只释放精确匹配的完整 Block；历史和审计保留；多人间父房 `已占/总床数` 的分子不包含维修来源。
- “内部占用”候选模型仍是房源、日期和原因，不显示住客、渠道、会员或金额，并以独立 `INTERNAL_USE` Block 表达；但这不是已确认经营事实，禁止在本阶段写入运行 schema、枚举或 UI。

**自动化门禁：**

- MAINTENANCE 的 DTO、权限、幂等、冲突和释放行为保持有效，并与正常/免费住宿执行整房和子床双向互斥。
- 维修不产生入住人数、Order、Stay、会员或资金事实。
- 静态与 contract 检查证明未确认的 INTERNAL_USE 没有被偷偷加入运行合同。

**用户人工验收：**

1. 在空闲房源创建一次维修锁房，确认只要求房源、日期和原因，不要求入住人、渠道、会员或金额。
2. 在多人间床位设置维修，确认父房住客占用比例不增加。
3. 释放完整维修，确认房态恢复且历史仍可查。
4. 确认页面没有把免费入住改名或暗中归类为“内部占用”。

**停点：** 维修锁房验收通过即可进入阶段 7。内部占用保持 `deferred_decision`；用户以后核定边界时再作为独立增量插入，不允许因此修改已通过的免费入住语义。

### 阶段 7：连续订单区块选择与订单上下文

**目标：** 工作人员点击房态中的已占订单或住客昵称时，一次选中该订单同一 Stay 的完整连续住宿，并在右侧直接看到订单概述和当前合法操作，而不是单日选区、报价草稿、schema 500 或技术信息墙。

**实施范围：**

- 修复 amendment payload 查询 DTO 与 schema 不一致导致的订单详情 500。
- 选择身份以服务端返回的稳定 `orderId`、`stayId` 或 typed `sourceReference` 为准，不以昵称相同、颜色相同、日期视觉相邻或客户端拼接格子判定同一订单。
- 点击已占连续区块、区块内任一天或该区块的住客昵称，均选择权威同一 Stay 的完整连续住宿。连续续住后的原日期和新增日期属于同一个订单选择容器，不拆成“原订单”和“续住订单”；容器内部必须保留可见、可查询的原始预订、续住、缩短和换房 segment/amendment 边界，不能渲染成一段无法审计来源的连续色块。
- 换房不打断连续 Stay；同一次住宿发生无缝换房时，同时高亮分布在原房源和新房源行上的全部住宿分段。不同订单、有至少一天日期间隔的新住宿、相邻但不同的 Stay，以及昵称相同的其他住客绝不能合并。
- 整房销售或整房订单的已占房间格提供该订单的明确入口。分床销售的父房格只汇总昵称与 `已占/总床数`，不提供含糊订单入口，也不任意选择第一笔订单；展开房间后，点击每个具体床位的已占格或昵称，必须提供并打开该床位对应的准确订单/Stay 入口。
- 若完整 Stay 部分落在当前可见日期窗口之外，只高亮当前窗口内的交集；右侧仍显示完整入住、退房日期、总晚数和全部住宿分段，并提供定位其他分段的操作。
- 右侧上下文明确分为来源模式：空白区间继续显示房源、日期和“创建正常住宿、创建免费入住、维修锁房”；已占正常或免费住宿切换为订单模式，维修切换为 Block 模式。未确认的内部占用不得出现入口。订单模式不得出现报价、会员搜索、住宿草稿日期、“应用选区”“清除选区”或创建订单控件。
- 房态页面以主表为首要工作区，响应式决策按房态实际可用内容宽度而不是未经扣减的浏览器 viewport 作出。只有主表和上下文都达到已验证的最小宽度时才固定双栏；典型笔记本和其他空间不足的桌面使用紧凑导航/间距/工具栏与可打开、可关闭的订单上下文抽屉，不要求浏览器缩放，不把右侧上下文简单移到主表下方形成超长往返。
- 日期列使用固定可读宽度，不能因点击订单或上下文模式切换而伸缩。自动日期窗口按主表实际宽度在 7 至 21 天之间计算，典型 1440px 笔记本约 10 天、1920px 大屏显示更多，并提供 `7 / 14 / 21` 显式档位；当当前查询不足所选档位时，只把结束日期补足到对应夜数，不进入 90 天阶段。
- 订单上下文抽屉必须具有明确的打开/关闭控件、可理解标题、焦点圈定、`Escape` 关闭和关闭后焦点恢复；打开、关闭及在空白选区/订单模式之间切换不得丢失当前日期窗口、筛选、展开、滚动位置、整段选择或草稿。短屏布局应让房态主表使用扣除真实顶部区域后的剩余高度，避免页面滚动与矩阵内滚动形成不可控的双重滚动。
- 订单上下文首先显示工作人员需要的“当前住宿安排”，再按时间顺序显示“变更记录”：订单号、主要居住人姓名与昵称、当前状态、正常/会员权益/免费入住标识、完整入住和退房日期、总晚数、当前房间/床位及全部住宿分段。每条变更记录展示变更前后日期/房源、原因、操作人员、时间、对应计价修订及相关收退款事实；点击记录只定位该次变更的日期段，不把它变成另一张订单。渠道、住宿金额、会员权益摘要和资金事实摘要仅按该订单真实适用内容只读展示。
- 默认房态不显示住宿文字横条；点击已占格或昵称后，仅用跨日期格的连续高亮表达完整 Stay，不在高亮上重复昵称、状态或订单号。订单号的工作人员可见位置收敛到右侧订单上下文和完整订单详情；该展示收敛不得改变稳定 `orderId`/`stayId` 关联、分段边界、点击定位或返回恢复。维修/锁房等 Block 横条继续显示。
- 根据服务端当前状态、权限和 `allowedActions` 只显示合法动作，并使用工作人员语言：办理入住、办理退房、提前退房或缩短住宿、续住、换房、取消订单、标记未到、登记收款、登记退款。客户端不得仅凭状态名称自行推导或放宽动作。
- “打开完整订单详情”保留为次要入口；房态右侧已经能够完成日常判断和找到下一步，不强迫工作人员先跳到技术详情页。
- 订单上下文提供“更正居住人资料”动作，允许人工更正该订单快照中的昵称、姓名、联系电话和证件号码。更正必须填写原因，服务端以追加式更正记录保留修改前后值、操作人和时间，房态及订单查询立即投影最新值；不得直接覆盖或删除原快照。会员住宿默认也只更正本订单居住人快照，不自动修改会员主档，会员主档如需变更必须从独立会员档案流程显式操作。
- 技术 ID、raw JSON、Preview、Confirm、Receipt、policy code、Lot/Coverage ID 移出工作人员主区域；稳定引用、审计和持久结果继续保留在 API 与获权审计入口。
- 订单上下文不展示“是否阻断、阻断事实、精确冲突、Claim”等协议字段。已占订单只用状态和住宿日期表达不可重复出售；空白选区遇到真实占用或异常时，以“哪个房源、哪段日期、为什么不能操作”的业务句子说明。
- 订单上下文必须查询权威 Order/Stay DTO；浏览器不得把相邻逐日格自行拼成住宿区间。必要时只做 additive DTO/OpenAPI 扩展，不破坏既有外围智能体合同。
- 后续阶段完成任一订单动作后必须刷新房态投影；若该 Stay 仍有效则保留整段选择及合理焦点，若状态或日期使原选择失效则用中文说明变化并返回可定位位置，该行为由阶段 8 至 13 分别验收。
- 房态已占区块和订单列表都能进入同一份业务详情；从完整订单详情返回时恢复原日期、筛选、展开、滚动位置、已选 Stay 和焦点。
- 本阶段只验收合法动作的可见性、中文标签和正确路由；入住、退房、续住、缩短、换房、取消、未到及收退款的表单、事务结果和动作后整段选择回归，分别由阶段 8 至 13 实施，不在本阶段提前夹带。

**自动化门禁：**

- 含多个 amendment、revision、coverage 和资金事实的真实订单均可序列化并打开。
- 点击多日订单任一天或昵称，都按稳定引用选中并高亮完整连续 Stay；连续续住后的原区间与新增区间一起选中，同时保留可识别的 segment/amendment 分界，且点击某条变更记录只能定位该段，不能改变订单选择身份。
- 同一 Stay 的换房分段能跨房源行共同高亮；相邻独立订单、存在日期间隔的新住宿和相同昵称订单均不合并。
- 选择已占订单时不发起 Quote 请求，右侧不存在报价、会员搜索、草稿日期或创建订单控件；选择空白区间时仍保留正确的创建/Block 动作。
- 点击逐日已占格或昵称仍按稳定引用打开正确订单；右侧显示订单号，默认房态与选中后的连续高亮均不重复显示住宿文字，Tooltip 和 accessible name 不显示订单号。
- 正常已预订/在住订单上下文不存在“阻断、Claim、冲突数量”等机器文案；冲突防护仍由服务端 DTO、Preview 重验和 PostgreSQL 互斥证明未被删除。
- 整房订单格解析到唯一订单；分床父房聚合格不暴露或猜测单笔订单入口，展开后的每个床位格分别按稳定引用解析到自身订单。
- 居住人资料更正覆盖昵称、姓名、联系电话和证件号码，要求非空原因并持久保留修改前后值、操作人和时间；直接覆写原快照、无原因更正、越权更正及会员主档隐式联动均失败关闭且零业务写入。更正成功后房态昵称、订单上下文和完整订单详情一致刷新。
- 完整 Stay 超出可见窗口时，高亮可见交集且上下文仍返回完整日期与所有分段，不由浏览器猜测窗口外事实。
- 无权字段正确 redaction；动作集合、禁用原因与服务端状态和权限返回完全一致，READ 主体看不到写操作。
- 使用预置事实模拟房态 projection 外部刷新后，有效选择保留、失效选择说明、从详情返回恢复和陈旧数据失败关闭均有 E2E；真正执行订单动作后的回归留在阶段 8 至 13。
- 鼠标、键盘、屏幕阅读器和触控可以选择同一订单区块；桌面、平板、移动、320 CSS px 和 200% zoom 无重叠或技术信息挤压。
- Playwright 几何与截图回归覆盖 `1280x720`、`1366x768` 和 `1440x800`：主表首屏至少可见 4 行，页面无横向溢出，资源列、日期头、筛选和日期控件可达；固定双栏不得把主表压到验收最小宽度以下。抽屉打开后内容不越界，键盘焦点受控，关闭后选区、滚动位置和触发元素焦点恢复。
- 1440px 自动模式约显示 10 个 94px 日期列；1920px 自动模式显示更多且不超过 21 天。点击订单、收起/重开普通或订单上下文前后日期列宽恒定，手动 `7 / 14 / 21` 及恢复自动均有 E2E。

**用户人工验收：**

1. 对整房订单点击已占房间格，确认右侧出现该订单入口；对分床多人间先确认父房聚合格不提供含糊入口，再展开房间，分别点击 `101A`、`101B` 等床位的已占格或昵称，确认右侧只出现该床对应的准确订单入口，并一次高亮同一 Stay 的完整连续日期。
2. 检查默认房态只在逐日格显示王羽昵称和必要状态、没有重复的住宿文字横条；点击后只出现整段连续高亮，右侧显示王羽这笔订单的订单号、基础信息、完整日期、总晚数、住宿分段和当前可执行操作，并确认不再显示报价、会员搜索、日期草稿、“应用选区”或“清除选区”。
3. 打开预置在真实 PostgreSQL 中、已经包含连续续住 amendment 的验收订单；分别点击原日期和新增日期，确认两者都选择同一个订单容器，但原始预订与续住区间仍有清楚边界；点击续住记录只定位新增日期段。
4. 打开预置的无缝换房验收订单，确认原房源和新房源上的同一 Stay 分段同时高亮，右侧按时间顺序列出全部分段。
5. 准备一笔相邻独立订单和一笔相同昵称订单，确认两者都不会与王羽的 Stay 合并。
6. 分别点击预置为不同状态的正常、会员和免费订单，确认适用摘要正确，且右侧只显示服务端为各自状态允许的动作入口。
7. 从右侧打开完整订单详情再返回房态，确认日期、筛选、展开、滚动位置、整段选择和焦点仍在原处。
8. 在 `1280x720`、`1366x768` 和 `1440x800` 验收房态：确认主表不再被固定右栏挤成狭小窗口、首屏至少可见 4 行；1440px 自动模式约显示 10 天，点击订单前后单日格不变大。打开订单操作抽屉完成查看，再用关闭按钮和 `Escape` 分别关闭，确认均回到原选区、滚动位置和焦点；在 1920px 下确认自动显示更多日期，并核对 `7 / 14 / 21 / 自动` 切换。
9. 在订单上下文更正昵称、姓名、联系电话或证件号码，填写更正原因后确认房态与订单详情立即显示新值，同时能查看修改前后值、操作人和时间；对会员住宿确认本次更正不会自动改动会员主档。

**停点：** 用户确认连续订单区块、跨房分段、订单上下文、返回恢复和动作入口均可理解后，才能进入阶段 8。

### 阶段 8：入住与退房

**目标：** 普通、会员和免费住宿都能从同一订单详情完成履约，房态用一致的人类语言准确反映“已预订 → 在住 → 已退房”；会员入住时正确核销权益，退房后正确释放库存。

**实施范围：**

- 订单尚未入住时，房态显示“已预订”且订单上下文显示“办理入住”；`CHECK_IN` 成功后，活动 Stay 区间立即改为“在住”且上下文显示“办理退房”。
- 正常和免费订单按状态更新 Stay；`CHECK_OUT` 在计划退房营业日按计划办理，晚于计划退房日则统一按原计划退房日迟录。成功后订单详情显示“已退房”，不再把订单表达为当前在住，并原子释放库存且不改写历史住宿安排记录或订单金额。
- 根据 2026-07-26 批准的 [`spec-disable-cleaning-workflow.md`](../docs/implementation/spec-disable-cleaning-workflow.md)，当前版本不新增、投影、展示或完成清洁任务；既有 `PENDING` 记录不进入房态或订单界面，清洁数据库结构、历史记录、命令契约、Receipt 与审计继续保留。
- 日期规则：本阶段只开放 `businessDate = arrivalDate` 的普通入住；`businessDate >= departureDate` 允许退房，其中大于时必须明确标为迟录并保留原计划退房日。未来入住、逾期入住和提前退房在阶段 9、10、12 关闭各自流程前由 Web、API、领域和数据库写入路径统一失败关闭并给出中文原因。
- 逾期在住订单只进入非阻断的待迟录退房异常任务；订单状态在办理前可保持“在住”，但房态不得在原计划退房日后合成住客昵称、在住区间或不可售状态。日期门禁隐藏入住/退房入口时，具体中文原因必须与订单操作位于同一视觉区域。
- CHECK_IN/CHECK_OUT 的业务结果必须能在 Query/OpenAPI 中按类型返回办理营业日、记录时间和操作人。它们是履约结果，不是住宿安排 segment；记录时间只表示工作人员何时办理，不得伪称为住客实际到店时间。当前无需为此改写历史 segment。
- 会员订单 CHECK_IN 时将现有 HELD coverage 转为 CONSUMED；CHECK_OUT 不重复核销。
- 页面用业务结果说明成功或失败，不展示 Confirm/Receipt 代码。

**自动化门禁：**

- `RESERVED -> CHECKED_IN -> CHECKED_OUT` 状态迁移、重复请求和非法状态零写入在 PostgreSQL 通过。
- 计划到店日前或计划到店日后调用普通 `CHECK_IN`，以及计划退房日前调用普通 `CHECK_OUT`，在对应后续流程完成前都必须失败关闭；在住订单也不得通过“先缩短到营业日、再普通退房”绕过提前退房原子流程。逾期退房则必须一次完成状态、amendment/Receipt/audit 和库存释放，保持原日期、金额、计价修订及会员权益不变。
- CHECK_IN/CHECK_OUT 的办理营业日、记录时间、操作人和业务结果可由 typed Query/DTO 读取；缺失或损坏的履约事实失败关闭，不回退为从 segment 或页面状态猜测。
- 会员 CHECK_IN 只核销仍为 HELD 的 coverage，CHECK_OUT 和幂等重放不重复核销。
- 正常与免费住宿的房态 typed source、昵称和多人间占用比例在 E2E 中正确变化。
- E2E 断言工作人员可见住宿状态按“已预订 → 在住 → 已退房”变化，退房后库存释放，且房态与订单界面不出现清洁任务、完成清洁入口、`RESERVED`、`CHECKED_IN`、`IN_HOUSE`、`CHECKED_OUT` 或“阻断库存”等内部状态词。
- 自动化证明当前退房零清洁写入、既有 `PENDING` 清洁记录不参与房态/订单投影、完成清洁命令失败关闭且不产生 Receipt 或业务写入。
- 订单详情的 allowed actions 与服务端状态机一致。

**用户人工验收：**

1. 分别对计划到店日的普通、会员和免费订单办理入住。
2. 确认房态由“已预订”立即变为“在住”，不出现另一套“已入住/入住中”同义状态，且住客昵称和床位占用比例正确。
3. 检查会员订单的权益在入住后已核销，普通和免费订单不触碰会员权益。
4. 在计划退房日办理普通退房，确认订单显示“已退房”、库存恢复可售，房态和订单界面均不出现清洁任务或完成清洁入口。
5. 对尚未到计划退房日的在住订单尝试退房及缩短到当天，确认页面明确说明当前版本暂不办理提前退房，且订单、日期、金额、库存和权益均不变化。
6. 对已超过计划退房日仍在住的订单办理退房，确认提交前显示“迟录退房”、原计划退房日、当前办理营业日和金额不变；完成后原日期与原金额不变、库存释放，订单可查询迟录结果且不产生清洁任务。
7. 入住或退房后返回房态，确认仍有效的 Stay 继续按完整区块选中；已结束而失效的选择得到明确说明和合理焦点。

**停点：** 用户确认入住/退房旅程通过后，才能进入阶段 9。

### 后续版本：清洁工作流重新启用（不计入当前阶段序号）

本节不属于当前阶段 8，也不进入或改变阶段 9 及其后续既定工作。清洁 schema、历史记录、命令契约、Receipt 与审计在暂停期间继续保留；未来重新启用前必须单独立项并重新确认：

1. 清洁任务的日期语义。
2. 跨营业日未完成任务的逾期呈现。
3. 清洁状态与库存可售、互斥规则的关系。
4. 查看、创建和完成清洁任务的权限边界。
5. 历史 `PENDING` 记录的启用范围、截止日期与批次处理政策。

### 前置纠偏 U1/U2（4.1 通过后、正式阶段 9 前）

为避免与第 4 步正式检查点 `4.2` 混淆，旧讨论名称“4.2A/4.2B”作废：共享命令外壳改称 U1，当前页面信息减负改称 U2。两者仍各自实施、测试、启动实例并等待人工验收。

U1 只收敛当前已经实现命令的核对、提交、恢复与结果反馈，不改变阶段 9 至 13 的未来业务规则。U2 在编码前先提交页面机器信息清单；订单详情按以下权威只读投影呈现：

1. **原始预订安排：** `CREATE_ORDER` 时的不可变入住日、退房日和房源。
2. **当前有效住宿安排：** 服务端沿 segment supersession 链计算的最新有效、不重叠日期/房源时间线。终态订单分别改称“最后住宿安排”“取消前安排”或“未到订单安排”。
3. **入住与退房结果：** 未办理入住、在住、已退房、已取消或未到，以及 CHECK_IN/CHECK_OUT 的办理营业日、记录时间和操作人。办理记录时间不得标成住客实际到店时间；未来需要补录实际发生时间时必须另建明确字段和权限。
4. **住宿安排变更历史：** 初始预订、改期、续住、缩短、换房和提前退房逐条显示前后日期/房源、原因、操作人、记录时间及相关计价/资金摘要。

底层 `stay_segments` 继续是不可变安排版本，不是实际入住流水。浏览器不得把所有 segment 顺序拼成实际住宿经历，也不得把最大 sequence 的 `currentSegment` 直接称为实际当前位置：未来换房显示“当前住宿位置”和“计划换至”。工作人员页面把 `INITIAL` 改为“初始预订安排”，移除 Segment/Amendment ID、版本号、raw payload、内部库存 ID 和英文技术类型。

U2 允许对 Query/DTO/OpenAPI 做 additive 只读扩展，但不新增或提前实现改期、续住、缩短、换房、取消、未到和资金命令。服务端投影损坏时继续失败关闭，不能由前端猜测当前安排或履约结果。

U2 同时收敛房态操作界面，但不改变任何订单动作的领域可用性：

- 点击空房态格时，在该格附近显示轻量快捷浮层，明确房源、日期和状态，只列出服务端当前允许的“创建住宿”“维修锁房”“查看房态记录”等入口；当前停用清洁时不得显示“房务”或清洁动作。
- 点击已占日期时，浮层显示住客、住宿周期、状态和准确订单入口；多人间父房聚合格涉及多笔订单时先显示简洁订单列表，不猜测单笔订单或写动作。
- 快捷浮层不承载完整表单。复杂查看进入非模态覆盖式抽屉，写操作进入阻止误触其他房态的表单抽屉；抽屉覆盖在右侧且不得压缩或移动房态表，桌面宽约 `420-480px`，标题与底部动作固定，移动端全屏。
- 关闭浮层或抽屉后恢复触发房态格、选区、页面纵向位置、时间轴横向位置和键盘焦点；后台刷新不得造成抽屉闪动、页面回跳或日期格变宽。
- 桌面左侧导航从当前 `210px` 收窄为约 `176px`，允许收起为约 `60px` 图标栏；按钮使用熟悉图标和名称提示，状态跨房态、订单、会员等页面保留，手机继续使用底部导航。
- 房态只保留一个主视图，通过搜索和筛选完成任务，不新增单日或渠道视图。工作人员页面统一使用“本单渠道应结金额”和“与政策基础金额差额”；内部 `CHANNEL_CONTRACT` 及领域、数据库语义不变。

**U2 自动化门禁：** PostgreSQL/contract 证明原始安排不变、当前安排无重叠、履约结果来自 typed 事实；订单经历续住、缩短、未来换房、退房、取消和未到时中文标签正确。桌面/手机 E2E 证明无 `INITIAL`、Segment ID、Amendments、raw payload、内部版本号或“渠道合同价”；在 `1280x720`、`1366x768`、`1440x800` 验证快捷浮层不越界、抽屉不改变主表宽度、左右导航状态稳定、关闭后焦点和双轴滚动位置恢复。键盘必须可打开浮层、进入抽屉并用 `Escape` 逐层关闭，200% zoom 和移动全屏不得重叠。

**U2 用户人工验收：**

1. 点击空房态格，确认快捷浮层在格子附近显示正确房号、日期和状态；只看到当前允许动作，不出现清洁或“房务”。
2. 点击整房订单、具体床位订单和包含多笔订单的父房格，确认前两者进入准确订单，父房先列出订单且不猜测单笔动作。
3. 从浮层打开一个查看抽屉和一个写操作抽屉，确认日历不被挤窄，标题/底部动作固定；关闭后回到原房态格、原选区和原双轴滚动位置。
4. 收起左侧导航，确认宽度明显缩小、图标名称可辨且切换页面后仍保持；重新展开后标签完整，手机底部导航不受影响。
5. 检查订单创建、概述、结果、详情和计价记录，确认只使用“本单渠道应结金额”和“与政策基础金额差额”，不再显示“渠道合同价”同义文案。

**U2 停点：** 启动独立可试用实例并等待用户明确回复 `U2 通过`；未通过时只修 U2，不得进入阶段 9/正式检查点 4.2。

### 阶段 9：未入住改期与在住续住

**当前状态：** `accepted`。用户已于 2026-07-29 明确回复 `4.2 通过`；本阶段已验收完成。阶段 10 也已于 2026-07-30 验收完成，当前工程入口为等待人工验收的阶段 11。

**目标：** 工作人员能在同一订单内调整未入住预订的入住日和/或退房日，并能延长已经入住的连续住宿；所有变化重新计算完整订单金额、迁移库存与权益，原安排和资金历史始终保留。

**实施范围：**

- 未入住订单提供“调整预订日期”，允许只改入住日、只改退房日或同时改期；新入住日不得早于当前营业日，新退房日必须晚于新入住日。操作保留同一 `orderId/stayId`，不得删除、取消或重新创建订单。
- 已入住订单提供“延长住宿”，只允许把退房日延后。计划退房日后仍在住时，退房动作统一按迟录处理；若实际需要继续住宿，工作人员必须在退房前主动选择延长住宿，系统不自动推断真实超期。
- 预检展示原日期、新日期、增减晚数、完整新晚数、原金额、政策重算金额、订单新金额、已登记净收款和差额，并要求独立住宿变更原因。
- 按变更后的完整有效住宿安排重新选档，继续使用成交时锁定的 pricing policy version；跨月不拆分。库存 Claim 在同一事务中释放旧范围差集并占用新范围差集，任一新日期冲突则全部回滚。
- 新 revision 不继承旧渠道价或旧手工目标价。外部渠道重新填写本次渠道应结金额，差异超过 15% 才要求渠道价格差异说明；WECOM 默认政策重算金额，主动偏离时人工调价原因必填；免费入住保持 0。
- 未入住会员改期释放删除日期的 HELD，并为新增日期重新形成覆盖/现金余量；已入住会员续住产生的新覆盖在同一事务立即从 HELD 转为 CONSUMED，不等待第二次 CHECK_IN。已 CONSUMED 权益不因改期或缩短恢复。
- 已有收款不会被日期调整覆盖；正负差额只作为后续人工补收/退款参考，实际资金事实仍由阶段 13 记录。

**自动化门禁：**

- 未入住订单提前/延后入住、提前/延后退房及整体平移均保持同一订单，追加安排版本、amendment 和完整 revision，旧事实不可变；当前实现缺少入住日改期命令，必须补齐 contracts/OpenAPI、领域、PostgreSQL、API、Web 和测试。
- 未入住改期与在住续住跨 7/14/30 夜档、跨自然月和允许跳档倒挂均使用原锁定政策版本。
- 新旧日期集合的库存 Claim 和 HELD coverage 原子迁移；并发冲突、陈旧确认、故障注入和幂等重放均无部分释放、重复占用或重复 revision。
- 外部渠道本次金额、15% 规则、WECOM 人工偏价、免费 0 元和会员现金余量遵守各自规则，不继承旧目标价。
- Preview 后库存、价格 basis 或权益变化时确认陈旧且零部分写入；幂等重放只返回原结果。
- 已入住会员的新增 coverage 在续住同一事务中立即 CONSUMED，不存在第二次 CHECK_IN 依赖。
- 免费改期/续住追加 amendment/revision/audit，金额为 0 且无会员权益事实。

**用户人工验收：**

1. 对五张独立未入住订单分别提前/延后入住日、缩短/延长预订安排和整体改期；确认订单号不变、原安排保留，新库存与金额一次生效。这里的缩短预订只是计划退房日提前，不是已入住后的提前离店。
2. 对已有收款的未入住订单改期，确认完整重价和补收/退款参考差额正确，但没有自动收退款。
3. 对未入住会员订单改期，确认删除日期只释放 HELD，新增日期重新覆盖；对已入住会员订单续住，确认新增覆盖立即核销。
4. 分别对外部渠道、WECOM 和免费订单改期/续住，确认本次渠道金额、15% 说明、人工调价和免费 0 元规则各自正确，旧目标价不自动继承。
5. 检查旧日期、旧金额和原始预订边界仍保留在变更历史中，新的计价修订可作为后续补收或退款事实的依据。
6. 续住成功后回到房态，点击原日期或新增日期，确认原区间与新增区间作为同一个连续 Stay 共同高亮，同时仍能看出并定位本次续住边界。

**阶段 9 已确认边界：** 若营业日已经晚于原计划退房日，系统不自动区分真实超期住宿与工作人员迟录。退房统一按原计划退房日迟录；继续住宿只能由工作人员主动发起延长，并接受库存冲突与重价校验。办理营业日和系统记录时间仍不表述为住客实际离店时间。

**停点：** 用户确认未入住改期、在住续住、完整计价和会员权益行为通过后，才能进入阶段 10。

### 阶段 10：缩短、提前退房与人工退款提示

**目标：** 已入住订单缩短后按新的完整住宿总晚数重选档位；若缩短至当前营业日，用一次业务确认原子完成提前退房、重价、释放未来库存和结束住宿，并只把正差额提供给工作人员作为人工退款参考。未入住日期调整统一由阶段 9 处理。

**实施范围：**

- 缩短输入新退房日期和本次原因，概述显示原日期、新日期、减少晚数、重算金额和 collectionDifference。`newDepartureDate > businessDate` 时缩短后继续在住；`newDepartureDate = businessDate` 且 `arrivalDate < businessDate` 时进入提前退房并释放营业日及以后库存；`newDepartureDate < businessDate` 时拒绝追溯改写。
- `arrivalDate = businessDate` 时不得办理提前退房、不得生成零夜安排，也不得借普通 `CHECK_OUT` 绕过。未实际使用房间的入住当天离店由阶段 12 的独立“撤销入住”处理；确认实际使用后的当天离店当前版本失败关闭。
- 在住订单于计划退房日前选择退房时必须进入“提前退房”流程，强制填写反映客户满意度或其他真实情况的离店原因；不得用“办理退房”等默认文本代替工作人员说明。
- 提前退房以门店实际营业日作为新的退房日期，并在一个事务中追加缩短/提前退房变更、形成新计价修订、立即释放营业日及之后的库存、把 Order/Stay 更新为已退房。任一步失败时全部回滚，普通 `CHECK_OUT` 不再承担提前退房语义。
- 使用原锁定政策版本，按缩短后的完整连续住宿晚数重新选 1/7/14/30 夜档。
- 新 revision 不继承旧渠道价或旧手工目标价；外部渠道重新确认本次渠道应结金额并执行 15% 规则，WECOM 主动偏价才要求人工调价原因，免费入住金额继续为 0。住宿变更原因始终独立必填，不能代替价格说明。
- 已经 CHECK_IN 核销的 CONSUMED 权益不得由缩短或提前退房恢复；本阶段不存在“尚未核销的未来房间”这一概念。
- 只有 `netRecordedCollection - newCurrentContractAmount > 0` 时显示“可供人工退款处理的差额”；系统不自动退款、不连接支付平台。
- 人工退款参考额严格沿用已确认口径：`max(0, netRecordedCollection - newCurrentContractAmount)`；`newCurrentContractAmount` 来自缩短后完整总晚数按原锁定政策重算并最终一次舍入的金额。
- 本阶段只显示退款参考，不写真实退款。真正退款在阶段 13 由工作人员引用原 COLLECTION 并录入退款自身 transactionReference；系统自动绑定当时适用的计价修订，工作人员不选择或填写内部 revision ID。
- 已完成的历史换房不阻断缩短；只要存在营业日当日或之后尚未生效的未来换房安排，本阶段失败关闭并显示中文原因，由阶段 11 原子裁剪换房时间线。

**自动化门禁：**

- 缩短后的完整总晚数重新落档，使用原政策版本，并且新 revision 不继承旧手工调整。
- `newDepartureDate` 相对营业日的大于、等于、小于三分支，以及入住当天提前退房门禁，均有领域、PostgreSQL、contract 和 E2E 证据；拒绝分支全量零写入。
- 提前退房的日期变更、重价、库存释放、已退房状态和 amendment/audit 必须同事务提交；故障注入、陈旧确认、并发占房和重复请求均不得留下部分结果。
- `netRecordedCollection` 高于、等于和低于新金额三种情况均保持 collectionDifference 原语义；只有正差额进入人工退款提示。
- 已 CONSUMED coverage 不恢复、不改写；提前退房不再次核销会员权益。
- 免费缩短保持 0 元并追加完整历史；并发冲突和幂等重放无重复 revision。

**用户人工验收：**

1. 对一笔在住订单把新退房日缩到营业日之后，确认订单继续在住、完整重价并只释放减少的未来库存。
2. 对另一笔在住订单把新退房日设为营业日，填写真实离店原因，确认新的实际结束日期、新金额、正退款参考额、库存释放和已退房状态一次完成；变更历史仍能看到原计划与本次提前退房。
3. 尝试把新退房日设为营业日前，确认拒绝追溯改写且订单、金额、库存和权益零变化。
4. 分别测试净收款高于和低于新金额，确认不会把负差额显示成可退款金额。
5. 对已入住会员订单缩短，确认已核销权益不会自动恢复。
6. 分别对外部渠道、WECOM 和免费订单缩短，确认本次价格说明规则正确，旧目标价不继承；确认页面只显示退款参考，并且本阶段没有新增退款事实。
7. 尝试对入住当天订单提前退房，以及对存在尚未生效未来换房安排的订单缩短，确认中文拒绝且订单、计价、库存、权益和资金零变化。

**阶段 10 已确认边界：** 入住当天不提供提前退房或钟点房模型，也不得形成零夜计价。未实际使用房间的“撤销入住”归阶段 12；4.3 只处理 `arrivalDate < businessDate` 的在住缩短/提前退房。退款只生成参考额，实际退款由阶段 13 登记。

**停点：** 用户确认缩短重算、权益和退款提示通过后，才能进入阶段 11。

### 阶段 11：换房

**当前状态：** `accepted`。2026-07-31 用户明确回复 `4.4 通过`；目标房源中文检索、抽屉稳定性和库存冲突提示纠偏也已复验通过。阶段 12 已于 2026-07-31 获用户确认通过。

**目标：** 换房保留同一个连续 Stay，并根据完整累计晚数对所有住宿分段重新计价。

**实施范围：**

- 工作人员选择换房生效日期、新房源和原因；目标列表与概述必须用中文房型、整房/具体床位和容量区分候选，不显示内部产品代码，也不能把 `104-A` 床位含糊表述成整间二人间。概述显示原房源、新房源、分段日期、原金额、新金额和差额；确认成功后当前订单上下文必须刷新为已提交的新金额，不能只在数据库形成 revision 而让页面继续显示旧金额。
- 换房不打断连续住宿；以当前有效连续安排的入住日至最终退房日累计总晚数选择一次档位，原始预订日期只保留历史且不重复计价。
- 各住宿分段按该统一档位下对应价格产品精确计算，所有分段相加后只对最终总价 half-up 一次。
- 同价产品换房金额不变；跨产品换房生成新的完整 revision 和 Stay segment。
- 整房与子床目标继续按完整剩余区间事务校验；不得自动换房或静默缩短。
- 免费入住换房金额保持 0；会员 coverage 遵守已核销不可隐式恢复规则。
- 新 revision 不继承换房前的旧渠道价或旧手工调价。外部渠道重新确认本次渠道应结金额并执行 15% 说明规则；WECOM 默认政策重算金额，主动偏离时人工调价原因必填；住宿变更原因仍独立必填。
- 未入住订单换房只是调整预订房源；已入住订单当天换房才表示实际搬房。未来生效换房在生效前显示“计划换至”，不得把最新 sequence 的 segment 直接当作客人实际当前位置；办理生效日和记录时间必须可追溯。
- 多房源未入住订单改期执行用户批准的方案 B：入住日与退房日等量平移时同步平移全部换房节点；单端或非等量修改保留新区间内原节点，以原首房/末房承接向前/向后扩展，完全早于/晚于旧安排的新区间分别由原首房/末房承接，最后合并相邻同房源段。操作员只填新入住日和退房日，Preview 显示完整计算结果。
- 床位与整房之间的换房验收当前只适用于现金订单和免费入住。会员订单仅允许权益类型匹配的同 kind 换房；BED_NIGHT 与 ROOM_NIGHT 不得静默转换，跨 kind 会员换房在用户确认新规则前必须失败关闭且零业务写入。

**自动化门禁：**

- 同价产品换房、跨价格产品换房、现金/免费订单的床位到整房，以及跨档累计晚数均由领域金标覆盖。
- 会员跨 BED_NIGHT/ROOM_NIGHT kind 换房以稳定错误拒绝，原订单、coverage 和库存保持不变。
- 所有分段精确求和后只对完整住宿最终总价 half-up 一次，不逐段舍入。
- 方案 B 的等量平移、单端修改、非等量修改、节点落边界及全部旧节点裁出均由 Unit 与 PostgreSQL 覆盖；冲突或损坏时间线失败时逐表零新增业务事实。
- 目标房源整房/子床冲突在真实 PostgreSQL 中拒绝且零部分写入。
- MOVE 只追加 segment、amendment 和 revision；原分段、旧 revision 和已核销 coverage 不被覆盖。

**用户人工验收：**

1. 同价与跨价换房：核对目标房源、连续分段、原金额、政策基础金额、新金额和差额，确认全部分段相加后只对最终总价舍入一次；成功后当前页面立即刷新。
2. 外部渠道、WECOM 与免费：外部渠道重新填写“本单渠道应结金额”并执行 15% 说明规则，WECOM 旧人工偏价不自动继承，免费保持 0；均不新增资金事实。
3. 当前/计划位置与组合日期动作：未生效换房显示“计划换至”，不改当前位置；已完成历史换房可续住，未来换房可在缩短时安全裁剪。
4. 床位/整房与会员：现金/免费订单按容量与完整目标区间验证；会员同 kind 合法换房，BED_NIGHT 与 ROOM_NIGHT 跨 kind 明确拒绝且权益零变化。
5. 房态追溯：从任一有效分段进入都选中同一 Stay，当前窗口内分段共同高亮；原安排、旧 revision 和换房原因仍可追溯。
6. 方案 B 多房源改期：等量整体平移后各分段晚数不变；单端或非等量改期保留新区间内换房日并正确裁剪/延长首尾；完全提前/延后分别使用原首房/末房。核对层的完整分段必须与完成后安排一致。

**停点：** 用户确认换房操作和分段计价通过后，才能进入阶段 12。

### 阶段 12：取消、未到与撤销入住

**目标：** 取消和未到清晰结束预订；入住当天确认未实际使用房间时可撤销入住。三者都保留原历史，并按各自语义释放库存、处理会员权益和形成退款参考。

**实施范围：**

- 只在合法状态显示取消或未到，并要求本次原因。
- `CANCEL_ORDER` 只用于尚未入住的订单；已入住后不复用取消，必须走缩短、提前退房或普通退房。
- 新增独立“撤销入住”命令，只允许 `CHECKED_IN`、`arrivalDate = businessDate`，并由工作人员确认未实际使用房间、填写真实原因。它不是普通取消或提前退房，订单对工作人员显示“入住已撤销”。
- 撤销入住保留原 CHECK_IN 和 CONSUMED 事实，追加不可变撤销/补偿事实；金额归零，当天及以后库存立即释放，会员权益通过补偿记录恢复，不能删除或改写原核销流水。
- 撤销入住已有净收款时显示 `建议退款 = max(0, netRecordedCollection)`；文案明确“仅供工作人员办理退款参考，目前尚未登记退款”，实际退款仍由阶段 13 登记。
- `MARK_NO_SHOW` 必须同时满足 `RESERVED` 状态，并且门店当地时间已到计划到店日 20:00；系统不得自动标记未到。到达该时点后仍允许工作人员选择补办入住或改期。
- 入住前取消/未到释放全部 HELD coverage 和未来库存 Claim。
- 已经 CONSUMED 的权益不得由普通取消、未到或缩短恢复；只有满足同日、未实际使用门禁的撤销入住可以追加补偿记录恢复。
- 免费入住取消同样追加 amendment/revision/审计，金额保持 0。
- 页面明确显示“已释放什么、未改变什么”，不使用含糊的“操作成功”。
- 今日履约必须显示 `arrival < businessDate`、仍为 RESERVED 且尚未超过离店日的应到未到订单，不能等到退房日之后才出现在异常中；入口可以归入清晰的“异常/待处理”队列，但具体动作仍由服务端 `allowedActions` 决定。

**自动化门禁：**

- 取消、未到和撤销入住使用独立订单夹具，分别验证状态、库存释放、HELD 释放、普通路径 CONSUMED 不恢复，以及撤销入住的补偿恢复与原流水保留。
- 非法状态、陈旧预检、重复请求和权限不足均失败关闭且零新增业务事实。
- 免费订单取消仍追加 amendment/revision/audit，金额保持 0 且不产生会员事实。
- `arrival = businessDate`、`arrival < businessDate <= departure`、`businessDate > departure` 三组预置订单在 TodayPage 与房态任务投影中的分类一致，不再存在只在一个视图可见的盲区。

**阶段 12 已确认边界：**

计划到店日当地时间 20:00 后允许工作人员标记未到，也允许补办入住或改期；系统绝不自动把逾期预订改成未到。付费订单取消或未到默认按全额退款计算退款参考额，并保留操作员调整退款金额的通道；状态、库存和 HELD 权益处理归本阶段，实际退款事实仍由阶段 13 人工登记。

**用户人工验收：**

1. 使用第一笔未入住普通订单执行取消，确认房态恢复可售。
2. 使用另一笔独立的未入住会员订单标记未到，确认冻结权益全部释放且核销为 0。
3. 对不允许取消或未到的状态，确认页面不显示错误动作。
4. 检查历史原因和原订单仍可追溯。
5. 把一笔应到订单保留为未入住直到下一营业日，确认它仍出现在工作人员待处理队列，而不是从所有当天视图消失。
6. 对入住当天、确认未实际使用房间的普通和会员订单执行撤销入住，确认真实原因、金额归零、当天库存立即可售、会员余额以补偿流水恢复，原入住与核销历史仍可追溯；已有收款只显示建议退款，没有自动退款。

**停点：** 用户确认取消/未到的库存和权益释放通过后，才能进入阶段 13。

### 阶段 13：人工收退款、订单资金摘要与住宿收款转会员

**目标：** 资金页面只表达 PMS 人工记录的经营事实，不暗示外部到账、结清或自动核销；在住宿资金基础收口后，提供受限、可审计且原子化的住宿收款转会员旅程。

**实施范围：**

- 每笔 COLLECTION 和 REFUND 分别录入自身 transactionReference；退款继续引用原 COLLECTION。服务端还必须自动保存该笔资金事实所依据的不可变 pricing revision，不要求工作人员查看或选择 revision ID。
- 工作人员端所有住宿收款、退款和调价金额统一按人民币“元”输入和显示，再用十进制定点规则精确转换为 API/数据库的 minor units；不得要求工作人员为 `¥1,280` 输入 `128000`，也不得用二进制浮点乘法制造分值误差。
- 订单详情完整列出多笔收款、退款和冲销，不在 Order 上设置单一会被覆盖的交易号；同一计价修订可以关联多笔分次收款或退款。历史资金事实若没有计价修订关联，Query 原样返回 `null` 并由 Web 派生显示“历史未关联”，不得虚构回填。
- 主摘要使用“住宿金额、已记录净收款、差额”中文名称，并保留既有三金额事实语义。工作人员在原始预订、续住、缩短或换房记录下看到的是“与本次变更相关的收退款”，而不是内部 ID 或会计分摊结果。
- 成功结果不强制进入 Receipt/Command 技术页；网络中断时用“正在查询刚才的操作结果”恢复原命令。
- 普通住宿不新增未经确认的支付渠道；会员订单已确认的企微收款只作为该业务的稳定事实。两者都不建设微信接口、Webhook、自动到账、自动退款或银行对账。
- 普通住宿现有 `method` 的真实业务含义尚未确认。实施时不得把订单来源 `WECOM` 当作支付方式，也不得因为会员订单只收企微而把普通住宿强制改成企微；在用户确认前保持现有数据兼容并把该字段隔离为局部 gate。
- 在资金输入、交易号和 `method` 边界关闭后，增加受限业务操作“将住宿收款转入会员订单”：目标会员订单必须关联选定 `memberId`，住宿主要居住人的身份证号必须与会员唯一身份证号一致；只能选择匹配会员产品房型的一笔或多笔完整原 COLLECTION，不允许拆分单笔收款、复制原交易号或建设通用跨订单分摊。
- 原住宿收款事实保持不可变；转出/转入以新的双向可追溯事实引用原 COLLECTION 和目标 `membershipOrderId`。同一笔收款只能成功转入一次，转入后不得继续作为住宿退款来源或再次计入另一会员订单。
- 只有已经明确记录为企业微信且有真实 `transactionReference` 的住宿收款可进入候选列表；不得从订单来源渠道推断支付方式。差额收款作为会员订单的新企微 COLLECTION，必须录入自身交易单号。
- 工作人员显式确认后，在一个 PostgreSQL 事务中完成：资金转入、会员订单生效、30 夜 Lot 发放、本次住宿的专用 conversion consumption、剩余权益计算，以及住宿订单的新计价修订。任何一步失败时全部回滚。
- 专用 conversion consumption 从新发 Lot 立即核销本次实际住宿 N 夜并逐日关联该 Stay，作为“办会员时用新权益结算本次住宿”的审计事实；它不是普通 coverageSet，不宣称会员在过去日期已经生效，也不能用于其他历史住宿。剩余权益仍只在会员订单生效后的有效期内使用。
- 住宿新计价修订保留政策基础报价与原现金计价历史，记录本次会员转换后 `cashRemainder=0`、`currentContractAmount=0`；原收款历史仍可见，但经转入后不能在住宿与会员订单两边重复计入。存在退款、冲销、超出会员成交价或已转入状态的收款先失败关闭。

**自动化门禁：**

- 新 COLLECTION/REFUND 缺少自身 transactionReference 或无法绑定本次 Preview 所依据的 pricing revision 时，在领域、数据库和 API 均拒绝且零写入；确认前 revision 变化必须按陈旧 Preview 拒绝。
- REFUND 必须引用同订单原 COLLECTION，同时保存退款自身交易号和本次依据的 pricing revision；REVERSAL 保持现有无交易号语义并通过被冲销 Fact 间接追溯。
- transactionReference 不施加未经确认的全局唯一约束，不接受 Fact/Receipt/Command/correlation/idempotency 标识代替；pricing revision 关联只表示本笔人工资金事实的业务依据，不得解释成分摊、到账、结清或核销。
- 幂等重放不重复产生资金 Fact；Query/Receipt/OpenAPI 能从原始预订或具体变更记录追溯到最终资金事实，也能从退款追溯到原收款。
- 金额解析覆盖整数元、角、分、非法精度、大额和空值；Web 显示、API minor units、数据库事实和 Query 回显精确往返，收 `¥1,280` 不会成为 `¥12.80` 或 `¥128,000`。
- 转会员的身份证同一人、房型匹配、整笔金额相等、防重复转入、转入后不可再作住宿退款来源、差额新交易号和原子回滚均在真实 PostgreSQL 通过；Query/Receipt/审计可双向追溯住宿收款与会员订单。
- conversion consumption 只允许命令关联的本次实际住宿，拒绝其他已退房订单或任意历史日期；它不写入普通 coverageSet、不回溯会员生效日，剩余权益有效期仍从生效时点起算一年。
- 金标示例通过：公卫单人间住宿 7 夜、原企业微信收款 `¥590`；整笔转入默认成交价 `¥1,620` 的公卫单人间会员订单，再收差额 `¥1,030`；发放 30 ROOM_NIGHT，专用事实立即核销本次 7 夜，剩余 23 夜；住宿新 revision 的 cash remainder/current amount 为 0，原现金计价、原收款和原交易号历史仍保留且不重复计入。

**阶段 13 的局部业务 gate：**

- 会员身份证号是唯一业务键，但历史住宿的主要居住人快照可能没有身份证号。实现不得只凭姓名或昵称自动合并；身份证号能够精确匹配的旅程可先完成，缺失身份证号的转会员必须失败关闭，直到用户确认受控关联方式。
- 普通住宿资金 `method` 的真实含义必须先由用户确认，才能据此筛选“已经明确记录为企业微信”的可转入收款；不得把订单来源 WECOM 代替资金事实。

**用户人工验收：**

1. 对一张企业微信来源订单分别为原始预订和一次续住按“元”录入收款，确认 `¥1,280` 无需输入 `128000`，两笔外部交易单号独立并分别显示在对应计价记录下；企业微信渠道订单号仍为空，页面也不因此臆造支付方式。
2. 缩短后引用其中一笔原收款发起退款，录入退款自身交易号，确认退款同时归入本次缩短的计价记录并能追到原收款。
3. 确认订单详情完整展示所有独立事实和业务关联，且没有“已到账、已结清、已核销”或会计分摊等推断。
4. 重复提交同一请求，确认不会新增第二笔 Fact。
5. 使用公卫单人间 7 夜、原收 `¥590` 的独立验收订单，选择身份证号一致的会员，创建默认价 `¥1,620` 的会员订单，整笔转入原收款并新收 `¥1,030`；确认会员剩余 23 夜、住宿当前金额和现金余量为 0，且两边都能查看同一条转入关系和原现金历史。
6. 再次转入同一笔收款、尝试拆分、选择身份证号/房型不匹配对象、使用其他历史住宿或在事务任一步制造失败，确认明确拒绝且没有半笔资金、会员订单、权益或重价事实。

**停点：** 用户确认资金事实、人工收退款和住宿收款转会员通过后，才能进入阶段 14。

### 阶段 14：30 天连续时间轴与超长住宿

**目标：** 以用户 2026-07-27 批准的连续 30 天方案替代旧 90 天草案，在任意日期锚点下提供可跨月的单一房态时间轴，并保持最长 366 夜住宿的完整校验。

**实施范围：**

- 以选定日期为起点连续加载 30 夜权威房态数据，可以跨自然月；左上角提供任意日期跳转、前移、后移和“今天”，移除 `自动 / 7 / 14 / 21`，不新增单日或渠道视图。
- 完整 30 天位于同一横轴，保持稳定日期列宽并左右滚动；房态表取消内部纵向滚动框，由整个页面负责上下滚动。日期头和左侧房源轴 sticky，视口底部提供与时间轴同步的横向滚动入口。
- 房源使用固定“楼栋分组 → 房间 → 可拆售床位”层级；床位只在支持拆售的房间下按需展开，房型作为辅助说明，不提供分类方式切换。
- 日期头用固定 `10-11px`、不换行的小字号显示 `剩18间 · 12床`。可售整房只统计可以按间下单的库存；可售床位只统计可以单独下单的床位；整房内物理床不得重复计入，单一类型筛选后只显示适用单位。
- 同一住宿的相邻逐日格用连续背景连接，每天仍显示昵称和必要状态，不增加第二条重复住宿文字横条；点击后整段高亮，超出当前 30 天的区间在边界显示前后延续标识。
- 30 夜以上至现有 366 夜边界的住宿通过侧边日期输入，不要求网格绘制完整区间。后台 Quote、工作人员订单概述和最终确认均校验完整住宿区间；第 30 天以后的冲突也必须精确返回。
- 时间轴交互不得重新引入“应用选区”“清除选区”或工作人员主动点击 Quote/Preview 的中间步骤；选区仍直接同步并自动显示住宿金额。
- 冲突可以定位到相应房源和日期，同时保留原长住草稿。
- 新版 30 天 companion 必须覆盖性能、无障碍、响应式、PostgreSQL、OpenAPI 和浏览器验收；时间轴必须保留 U2 已通过的窄导航、快捷浮层、覆盖式抽屉和典型笔记本主表优先语义。

**用户人工验收：**

1. 选择月末日期，确认加载从该日开始且可跨月的连续 30 天；使用前移、后移、今天和任意日期跳转，并确认不再出现 `自动 / 7 / 14 / 21`。
2. 横向浏览 30 天、纵向浏览全部房源，确认只有时间轴左右滚动，整页负责上下滚动，日期头、房源轴和视口底部横向入口稳定可用。
3. 展开一个可拆床房间，确认楼栋、房间和床位关系；逐日核对 `剩 X 间 · Y 床` 与真实库存一致，筛选后单位正确且无重复统计。
4. 查看一笔跨多日并越过当前窗口边界的住宿，确认每日昵称保留、相邻日期连续、点击后整段高亮，边界显示延续方向。
5. 在侧边输入 `2026-07-26` 至 `2026-11-20`，确认显示 117 夜；制造第 30 天以后的冲突，确认服务端拒绝并可跳转查看具体日期。
6. 检查 `1280x720`、`1366x768`、`1440x800`、1024、768、375、320 CSS px 和 200% zoom，确认余量不撑大单元格、整页滚动无重叠，并继续保持 U2 通过的窄导航和覆盖式抽屉。

**停点：** 该 companion 的全部 Definition of Done 及用户人工验收通过后，才能进入最终回归。

### 阶段 15：完整旅程回归与发布验收

**目标：** 证明所有分步改动可以组合成同一套稳定经营流程，而不是各阶段孤立演示。

**用户人工验收（完整旅程）：**

```text
查询可售
  -> 直接选房和日期
  -> 自动显示住宿金额
  -> 可选会员覆盖
  -> 创建并确认一次订单
  -> 分次记录收款
  -> 入住
  -> 续住
  -> 换房
  -> 缩短并重计价
  -> 引用原收款人工退款
  -> 退房
```

另行完整走通：

- 免费入住：住客资料 -> 免费类型和原因 -> 创建 -> 入住 -> 续住/换房/缩短 -> 退房；
- 会员建档与购买：会员列表 -> 建档 -> 匹配产品 -> 多笔企微收款 -> 显式生效 -> 30 夜发放 -> 余额与不可变交易记录；
- 会员入住：冻结 -> 入住核销 -> 已入住续住即时核销新增覆盖 -> 取消/缩短只释放 HELD；
- 住宿转会员：选择同住客匹配房型 -> 整笔转入原住宿收款 -> 新收企微差额 -> 原子生效与核销 -> 双向审计；
- 维修锁房：设置 -> 房床互斥 -> 释放；内部占用只有在用户以后确认经营边界后才加入验收，不是当前发布门禁；
- 连续订单区块：续住后点击原日期或新增日期均选中完整区间；换房后点击任一分段均同时高亮原房源和新房源分段；
- 外围智能体：受限 Token 查询 -> Preview/Confirm -> 幂等重放 -> 中断恢复。

**发布前工作人员体验门禁：**

- 今日履约可以看到应到、逾期未到、在住、离店和异常任务；不会因前端过滤让逾期仍 RESERVED 的订单暂时消失。
- 损坏或不可读的本地命令恢复记录只影响当前浏览器主体/物业范围，并提供先查询服务端结果、再受控处置的中文路径；不得让工作人员以“清缓存后重试”制造重复业务事实。
- 登录页只在明确 demo 环境展示演示账号，生产构建不预填或公开密码；Token 管理入口按实际权限显示，视觉分组不代替服务端授权。
- Today/Orders/Room Status 刷新或投影更新时保留正在编辑的表单、当前选区、滚动位置和键盘焦点；错误确认按钮按动作风险使用正常或危险语义，不把所有写操作统一显示成红色。
- Room Status 的发布截图与几何回归包含 `1280x720`、`1366x768`、`1440x800`；主表首屏行数、页面横向溢出、抽屉焦点与关闭恢复必须继续满足阶段 7 的已验收合同。
- 普通住宿资金 `method` 已按 4.6 确认形成 contract：企业微信/银行转账记录交易号，现金记录收款人，其他记录必填说明；企业微信退款原路退回。READ Token 可见的个人资料范围和交班汇总是否纳入 MVP 仍待后续决定，未确认前不得自行扩大权限。清洁与可售关系已移入“后续版本：清洁工作流重新启用”，不再阻断当前版本。

**最终自动化门禁：**

```bash
npm run verify
npm run test:integration
npm run test:contract
npm run build
npm run test:pricing-facts
npm run test:e2e
```

还必须完成真实 PostgreSQL 并发互斥、陈旧预检零写入、重复请求、命令恢复损坏处置、备份恢复、readiness、Axe、键盘、响应式和浏览器截图检查。

**停点：** 用户完成最终人工验收前，不把本计划标记为 `done`。

## 8. 阶段状态表

| 阶段 | 名称 | 状态 | 人工验收 |
|---:|---|---|---|
| 1 | 选房后自动显示住宿金额 | accepted | 2026-07-23 用户明确回复“第 1 步通过”；按当前检查点记录为阶段 1 验收通过 |
| 2 | 正常订单概述、渠道应结金额与一次确认 | accepted | 2026-07-27 用户确认五组渠道计价人工测试均无问题；渠道订单号/合同金额、15% 说明门槛、WECOM 直订偏价原因和 CREATE_ORDER 原子首个 revision 验收通过。TypeScript、Unit `300/300`、Integration `182/182`、Contract/OpenAPI `57/57`、阶段 2 桌面与手机 E2E `6/6`、production build 均通过 |
| 3 | 免费入住 | accepted | 2026-07-23 随第 1 步人工验收通过：免费入住可从选房到创建完成 |
| 4 | 会员档案、会员订单与会员入住 | accepted | 2026-07-24 用户明确回复“第 2 步通过”；2A、2B、2C 全部通过 |
| 5 | 多人间与整房首页显示全部住客昵称 | accepted | 2026-07-24 用户完成复验并回复“检查完了，继续”；允许进入阶段 6 |
| 6 | 维修锁房与内部占用决策 | accepted | 2026-07-25 随第 3 步合并验收通过；内部占用继续保持 deferred_decision |
| 7 | 连续订单区块选择与订单上下文 | accepted | 2026-07-25 用户明确回复“第三步通过”；阶段 8 未开始 |
| 7R | 换房后 Stay 选择回归 | accepted | 2026-07-27 用户明确回复“7R 通过”；换房前后双向选择、详情返回后的最新位置恢复、同名订单隔离和损坏事实失败关闭通过人工验收。Unit `306/306`、Integration `182/182`、Stage 7 桌面/手机 E2E `13 passed / 13 expected skipped`、TypeScript、production build 与补丁格式均通过 |
| U1 | 前置纠偏：共享命令外壳 | accepted | 2026-07-28 用户逐项确认 5 项人工验收均为 `OK`；入住/退房单层核对与可选备注、房态桌面/手机内联办理、调价默认当前金额及权限/revision 竞态均通过 |
| U2 | 前置纠偏：当前页面信息减负 | accepted | 2026-07-29 用户明确回复 `U2 通过，开始 4.2`；单击与拖选统一快捷操作框、下方/上方定位、触发行避让、完整范围传递、双轴与焦点恢复通过人工验收 |
| 8 | 入住与退房 | accepted | 2026-07-26 用户明确回复“4.1 通过”；入住、计划退房、逾期迟录、会员权益核销、库存释放、停用清洁与日期门禁人工验收完成 |
| 9 | 未入住改期与在住续住 | accepted | 2026-07-29 用户明确回复 `4.2 通过`。六组人工验收、验收缺陷修复及验收后独立审计完成；日期变更写路径会在 Preview/Confirm 写入前复用完整生命周期校验，已核销会员在合同失效后续住时保留原权益事实并仅按新增夜形成现金余量。Unit `430/430`、Integration `202/202`、Contract/OpenAPI `59/59`、pricing facts `7/7`、Stage 9 E2E `8 passed / 8 expected skipped`、TypeScript、production build 与补丁格式通过 |
| 10 | 在住缩短、提前退房与人工退款提示 | accepted | 2026-07-30 用户逐项确认六项人工验收并明确回复 `4.3 通过`。旧抽屉失效、统一退房日期入口、提前退房、外部渠道应结语义、合法后续调价和会员产品追溯均通过；阶段 11 待开始 |
| 11 | 换房 | accepted | 2026-07-31 用户明确回复 `4.4 通过`；目标房源纯中文检索、真实床位父房型、抽屉轮询稳定性、准确容量/库存拒绝及冲突零写入均完成复验 |
| 12 | 取消、未到与撤销入住 | accepted | 2026-07-31 用户明确回复 `4.5 已通过`。取消、未到、迟录入住、撤销入住、会员补偿恢复、移动端取消代表用例和 U2 文案/布局纠偏均通过人工验收；实际退款仍归阶段 13 |
| 13 | 人工收退款、订单资金摘要与住宿收款转会员 | accepted | 2026-08-02 用户明确回复 `4.7 通过`；4.6 与 4.7 均完成人工验收，阶段 13 收口。 |
| 14 | 30 天连续时间轴与超长住宿 | accepted | 2026-08-02 用户完成人工验收：30 夜连续跨月、楼栋/房间/床位层级、整页滚动、超长住宿完整日期校验、冲突日期提示及右侧抽屉滚动均通过；阶段 14 收口 |
| 15 | 完整旅程回归与发布验收 | accepted | 2026-08-03 用户完成最终集中人工验收并确认通过；四个小步骤、A/B 浏览器证据、机器发布门禁、最终独立审计、两条共存金标旅程、Token/API 与报价外部字段表达均已收口 |

已确认范围的状态只允许按 `pending -> in_progress -> awaiting_user_acceptance -> accepted` 推进。阶段 6 的内部占用子项允许保持 `deferred_decision`，且不阻断阶段 7；任一已实施阶段被用户退回时保持 `in_progress`，记录问题并重新验收，不得把其他后续阶段标为开始。

## 9. 实施约束

- 当前 Green PMS 目录是唯一写入范围，不读取或复制任何旧 PMS/FewohBee。
- 保留当前工作树和用户已有文件；不得重置、覆盖或顺手清理无关改动。
- 一个热点模块同一时间只有一个写入负责人；Subagent 可并行做只读分析、独立测试和审查。
- 每阶段优先复用现有 contracts、Command service、Quote、pricing、coverage、Receipt recovery 和 RoomStatusGrid，不另建第二条业务写入路径。
- 工作人员文案可以隐藏技术协议，但所有失败关闭、授权、幂等、事务、审计和恢复保证必须保留。
- 不把技术字段改成中文字符串持久化；稳定 code、ID 和 DTO 继续用于 API 与数据库。
- 不把会员权益解释成免费、折扣或现金价值。
- 不把已录入交易号解释成外部已到账、已结清或已核销。
- 不自动换房、不自动退款、不自动对账、不自动恢复已核销会员权益。
- 住宿收款转会员只允许用户已确认的“同住客、匹配房型、当前有效住宿收款整笔且整单闭环、原子转入”路径；不得据此建设通用跨订单分摊或任意转账。
- 不在阶段 14 之前扩大时间轴范围；前 13 个阶段使用现有日期窗口完成聚焦验收。

## 10. Non-Goals

- 不建设会员自助下单页面；
- 当前版本不处理会员合同退费；在合同拟定前不实现退费公式、退款命令，也不在页面作法律意义的不可退款承诺；
- 不建设支付平台、微信支付、Webhook、银行对账或会计模块；
- 不建设通用动态定价、低代码规则或工作人员可编辑公式；
- 不新增“未知/历史”渠道或虚构历史业务值；
- 不把免费入住和内部占用合并；
- 不在经营边界确认前实现内部占用枚举、表单或命令；
- 不默认加载 180/365 天或建设无限时间轴；
- 不用技术审计页面代替工作人员订单概述；
- 不用文档、线框图、静态截图或 mock 代替可运行实现。

## 11. 最终完成条件

只有以下全部满足，才能将本文件 frontmatter 的 `status` 改为 `done`：

- 15 个阶段均为 `accepted`；
- 用户可在同一运行实例完成正常、会员、免费和维修锁房旅程；内部占用只有在用户另行确认后才成为完成条件；
- 会员统一列表、身份证唯一匹配、三种有限会员产品、多笔企微收款、显式生效、账本余额和会员住宿旅程完整可用；
- 普通实时办卡的会员产品有效期从工作人员点击生效起算一年；第 9 步管理员历史办卡补录和生效日纠正是证据完整、保持当前有效状态且不改变资金/权益余额的窄例外。当前版本不执行会员退费，旧 PAYMENT_DATE/NON_REFUNDABLE 运行语义不再作为普通办卡入口的自动推断；
- 住宿收款转会员的整笔、防重复、差额新交易号、30 夜发放、本次核销和双向审计在同一事务通过；
- 未入住订单调整入住日/退房日、在住续住、在住缩短、提前退房、换房、取消、未到、普通入住和普通退房均可从工作人员能找到的入口完成；任何改期都保留同一订单并追加历史，不通过删单重下模拟；
- 状态乘营业日期矩阵已关闭未来入住、提前退房旁路、追溯缩短和未到时点边界；逾期退房按不可变原计划日期迟录；所有未批准分支失败关闭且零写入；
- 主流程不再出现多余应用选区、住宿类型选择、主动获取服务端报价或技术确认页；
- 多人间父房逐日格直接显示全部有权住客昵称和 `已占/总床数`，不再出现“首个昵称 +N”；
- 正常已预订/在住格、Tooltip、屏读名称、移动任务和订单上下文不显示“阻断、Claim、冲突数量”等机器语言，底层库存互斥与冲突事实仍完整有效；
- 办理入住后房态从“已预订”变为“在住”，办理退房后订单显示“已退房”且后续库存按库存事实释放；当前版本不生成、投影、展示或完成清洁任务，不混用内部枚举或多套中文同义状态；
- 点击已占日期或昵称按稳定引用选中完整连续 Stay；连续续住与无缝换房分段不会被拆散，右侧显示权威订单概述和服务端允许的操作而非报价草稿；
- 订单详情分别显示原始预订安排、当前/最后有效安排、入住与退房结果和住宿安排变更历史；不把全部不可变 segment 当作实际入住流水，不显示 `INITIAL`、Segment/Amendment ID、原始 payload 或内部版本号；
- 30 天连续时间轴和超长住宿完整区间校验通过；
- D/E 楼运营房号统一为 `D01-D05`、`E01-E03`，44 房/91 床/77 基础库存闭合不变；
- 工作人员资金表单按人民币元输入和显示；今日履约无逾期未到盲区；损坏的本地恢复记录有失败关闭且可执行的安全处置路径；
- Web/API 共享领域规则，OpenAPI 和外围智能体合同无回归；
- unit/domain、PostgreSQL integration、contract、pricing facts、E2E、build、备份恢复和 readiness 全部通过；
- 桌面、平板、移动、320 CSS px、200% zoom、键盘和 Axe 验收通过；
- 核心实现没有 mock、占位逻辑或未关闭关键 TODO；
- 提供最终 URL、账号、测试数量、截图路径、commit、push 状态和剩余非阻断风险。

## 12. 计划变更记录

- 2026-08-02：用户确认阶段 14 全部人工验收项完成；阶段 14 标记为 `accepted`，阶段 15“完整旅程回归与发布验收”开始。
- 2026-08-01：用户要求开始 4.7：住宿收款整笔转会员。建立 [4.7 A 级实施规格](./QinTopia-PMS-第4步-4.7-实施规格.md)，阶段 13 保持 `in_progress`。本切片只实现普通住宿有效企业微信收款整笔转入新会员订单、差额新企微收款、会员订单生效、30 夜发放、本次住宿专用核销、住宿金额归零、双向追溯和失败零写入；不进入会员退款、渠道总账、微信接口、旧住宿无身份证号受控关联、手机房态首页或阶段 14 时间轴。
- 2026-08-02：4.7 技术收口与自动测试补强完成。补强点包括：升级会员入口只对企业微信来源普通住宿开放；外部渠道、非企业微信来源、混合资金、无完整可转入企业微信住宿收款提前禁用；取消 20 笔收款人为上限；核对页展示用于升级的住宿收款明细；成功页和订单/会员追溯使用业务语言；订单详情会员权益覆盖能显示升级会员产生的专用核销；数据库迁移补强已冲销/重复转入等防线。门禁结果：TypeScript、前端展示单测、完整 Integration、完整 Contract 与补丁格式均通过。
- 2026-08-02：用户明确回复 `4.7 通过`。阶段 13（人工收退款、订单资金摘要与住宿收款转会员）完成并标记 `accepted`；进入阶段 14“30 天连续时间轴与超长住宿”。
- 2026-08-02：建立 [阶段 14 30 天权威规格](./房态30天连续时间轴与超长住宿开发规格.md)，明确 30 夜查询窗口、整页纵向滚动、房源层级、超长住宿完整区间校验和人工验收门禁；旧 90 天规格保留为历史草案，不再作为实施依据。
- 2026-08-01：用户确认 4.6 人工验收通过。基础资金登记收口为：普通前台收款默认企业微信，可选银行转账、现金、其他；企业微信和银行转账必须登记交易号/流水号，现金必须登记收款人，其他必须填写其他收款说明；外部渠道订单不提供单笔收退款入口，只展示渠道订单号和当前应结金额；退款必须逐笔引用原收款，企业微信退款沿用原收款交易单号原路退回，超出所选原收款剩余可退金额时显示中文额度提示。手机房态首页信息结构与 30 天时间轴/移动端 UI 收口留到阶段 14 及最终移动端收尾，不阻塞 4.6。阶段 13 继续保留 `in_progress`，等待 4.7 住宿收款整笔转会员。
- 2026-07-31：用户要求开始 4.6 / 阶段 13，并调整项目后续 sub-agent 模型偏好：开发/执行型优先 GPT 5.5 High，审计类用 GPT 5.6 sol；当前工具未暴露 GPT 5.5 时以 `gpt-5.6-terra` High 临时代替并汇报。阶段 13 转为 `in_progress`，建立 [4.6 A 级实施规格](./QinTopia-PMS-第4步-4.6-实施规格.md)。4.6 只覆盖多笔收退款、交易号、按元录入和变更关联；住宿收款整笔转会员保留到 4.7。用户确认企业微信来源订单登记为企业微信收款并记录交易单号；外部渠道仍不推断平台单笔到账。历史资金 revision 关联按兼容读取处理。
- 2026-07-31：用户明确回复 `4.5 已通过`。阶段 12 转为 `accepted`，取消、未到、迟录入住、撤销入住、会员权益补偿恢复、移动端代表取消用例及“当前日期范围占用明细”说明/手机房态文案纠偏均完成验收。阶段 13 资金登记仍未开始。
- 2026-07-30：阶段 11 / 4.4 完成并转为 `awaiting_user_acceptance`。`MOVE_UNIT` 完整重价、外部渠道/WECOM/免费/会员规则、当前/计划位置、方案 B 组合改期、migration 028、Web 核对和即时刷新已端到端落地。全量自动门禁、真实 027 -> 028 恢复、readiness、冷启动和独立复审通过；独立实例 `http://127.0.0.1:4283/` 等待人工验收。未收到 `4.4 通过` 前不提交，阶段 12 仍未开始。
- 2026-07-30：用户批准阶段 11 多房源未入住订单改期方案 B。入住日与退房日等量平移时同步平移全部换房节点；只修改一端或移动量不同时，保留新区间内原换房日并裁剪/延长首尾。操作员只填写新入住日和退房日，Preview 展示系统计算后的完整分段；实施前 Gate 关闭，开始 T1。
- 2026-07-30：4.3 提交 `c15c9c4` 后启动阶段 11 / 4.4，建立 [4.4 A 级实施规格](./QinTopia-PMS-第4步-4.4-实施规格.md)。用户批准多房源未入住订单改期方案 B，规格 Gate 已关闭；当前正在实施 `MOVE_UNIT` 完整价格决策、渠道/WECOM、当前/计划位置、会员写入边界、Web 核对、migration 028 和组合日期算法，阶段 12 未开始。
- 2026-07-30：用户逐项确认 4.3 六项人工验收并明确回复 `4.3 通过`；阶段 10 转为 `accepted`，工程入口推进到 `stage_11_pending`。本次只关闭 4.3，不提前标记阶段 11 为已实施。
- 2026-07-30：阶段 10 / 4.3 的 T7 人工验收纠偏完成并重新转为 `awaiting_user_acceptance`。五项审查修复覆盖合法 `POLICY` 后续调价 DTO、外部渠道不显示逐单收退款、coverage 为空时按会员合同追溯产品、Stage 10 与共享 E2E 房源隔离，以及旧订单延迟请求显式等待 200/取消终态；联合回归同时固定跨阶段幂等键、4.1 恢复入口和 4.2 刷新后房态选区时序。最终门禁为 Unit `498/498`、Integration `224/224`、Contract/OpenAPI `62/62`、pricing facts `7/7`、U2/Stage 8/Stage 9/Stage 10 联合关键 E2E `37 passed / 37 expected skipped`、TypeScript、production build 与 `git diff --check`。4.4/4.5 未开始。
- 2026-07-29：阶段 10 / 4.3 完成并转为 `awaiting_user_acceptance`。在住缩短、原子提前退房、完整重价、退款参考、库存差集、会员已核销权益不恢复、渠道/WECOM/免费规则和历史/未来换房边界已端到端实现。独立审查发现并修复未来换房时间线伪造、退款参考溢出、非 `APPLIED` 命令挂载业务组合、readiness/恢复漏查 Stage 10 触发器及绑定函数等阻断；真实 populated 026 备份恢复到完整 027 通过。最终门禁为 Unit `452/452`、Integration `224/224`、Contract/OpenAPI `62/62`、pricing facts `7/7`、Stage 10 E2E `10 passed / 10 expected skipped`、TypeScript、production build、shell syntax 与 `git diff --check`。独立实例为 `http://127.0.0.1:4240/`；阶段 11 及以后未开始。
- 2026-07-29：4.2 验收后独立审计发现两个提交阻断并完成闭环：（1）Direct Preview 原可绕过订单 Query 的完整住宿生命周期校验，现由 `RESCHEDULE_STAY` / `EXTEND_STAY` 在构建 effect 前统一验证 Stay 状态、amendment/version 链、当前 pricing revision 指针和 segment supersession 链，四类损坏事实均以业务表、Preview、Command、Receipt 与 Audit 零新增证明失败关闭；（2）已入住会员的原覆盖已经核销但合同随后失效时，续住原被错误拒绝，现保留既有 CONSUMED 覆盖并只对新增夜形成现金余量，新预订及未入住会员仍不能使用失效合同。最终独立复核无阻断，门禁为 TypeScript、Unit `430/430`、Integration `202/202`、Contract/OpenAPI `59/59`、pricing facts `7/7`、Stage 9 E2E `8 passed / 8 expected skipped`、production build 与 `git diff --check`；阶段 10 仍未开始。
- 2026-07-29：用户明确回复 `4.2 通过`。阶段 9 转为 `accepted`，工程入口推进到仍未开始的 `stage_10_pending`。六组人工验收覆盖未入住改期、在住续住、完整重价、库存/权益迁移、渠道/WECOM/免费规则、多房源失败关闭和资金差额提示；验收修复只收敛当前 4.2 的重价可见性、金额控件与文案、日期抽屉业务上下文及“今天”底色。真实提前退房仍归阶段 10，多房源未入住组合改期仍归阶段 11。
- 2026-07-29：阶段 9 第六组人工验收确认 D02 与 202 没有订单或 Stay 串联；操作员误认源于横向滚动后左侧房号不可见，而日期抽屉没有住客/房号/状态，并把当前有效周期误称为“原预订日期”。本轮只修复日期抽屉的固定业务上下文和 E2E，不改变续住、计价、库存或房态投影。今天列标记真实营业日期，历史窗口中位于中间属于正确语义；用户明确要求当前版本使用美观底色，因此本轮同时移除贯穿全表的粗竖线，改为日期头与整列的轻量品牌色底色并保留“今天”文字，不改日历时间轴结构。
- 2026-07-29：阶段 9 第一轮人工验收发现改期抽屉第一层无法看见服务端重价结果、企业微信金额说明技术化且复选框被全局输入样式异常放大；验收样例还把未入住订单的“缩短预订”误称为“提前离店”。阶段 9 仅修复当前 4.2：复用既有服务端 Preview 在第一层显示原金额、调整后金额和变化，冲突时禁止继续；收敛企业微信文案和金额开关；更正样例名称。真实提前离店仍归阶段 10 / 4.3，阶段 10 未开始。
- 2026-07-29：阶段 9 / 4.2 完成并转为 `awaiting_user_acceptance`。`RESCHEDULE_STAY` 与收紧后的 `EXTEND_STAY` 已端到端覆盖 contracts/OpenAPI、领域、PostgreSQL、API、Query、Web 与恢复协议；完整新区间重价、Claim 与会员权益差集、渠道/WECOM/免费规则、多房源方案 A、损坏事实及陈旧 Preview 均失败关闭。最终门禁为 TypeScript、Unit `430/430`、Integration `197/197`、Contract/OpenAPI `59/59`、pricing facts `7/7`、Stage 9 E2E `7 passed / 7 expected skipped`、production build 与 `git diff --check`。联合 UI 回归的高负载五秒时序失败均有独立通过证据，并收紧为显式房态刷新或 15 秒最终状态等待，不改变业务新鲜度规则。独立实例 `http://127.0.0.1:4321/` 使用 `qintopia_stage9_acceptance`；阶段 10 及以后未开始。
- 2026-07-29：用户明确回复 `U2 通过，开始 4.2`。U2 转为 `accepted`，阶段 9 / 正式检查点 4.2 转为 `in_progress`；完成六份权威文档和现有代码主干复核，并新增 A 级[4.2 实施规格](./QinTopia-PMS-第4步-4.2-实施规格.md)，冻结 `RESCHEDULE_STAY`、收紧后的 `EXTEND_STAY`、完整重价、Claim/会员权益迁移、事务回滚、测试与独立验收数据边界。阶段 10 及以后保持未开始。
- 2026-07-28：U2 第二轮返工完成并重新转为 `awaiting_user_acceptance`。单击一格、鼠标拖选和键盘扩展区间统一先显示同一轻量快捷操作框，选择创建住宿、维修锁房或查看房态记录后才进入既有抽屉流程；操作框宽度不超过 `280px`，优先位于完整触发行下方、空间不足时位于上方，并按真实内容高度重新定位，标题和范围摘要不截断。最终门禁为 TypeScript、production build、`git diff --check`、Unit `413/413`、U2 桌面 `9 passed / 1 expected skipped`、U2 移动端 `1 passed / 9 expected skipped`、Stage 1 连续拖选 `1/1`、Stage 7 双工作台维修状态回归 `1/1`；多轮只读审查发现的真实补丁问题均已修复。独立验收库 `qintopia_u2_acceptance` 未重置，实例继续使用 `http://127.0.0.1:4231/`；未收到 `U2 通过` 前不启动正式 4.2。
- 2026-07-28：U2 第二轮人工验收发现单击一格先显示快捷操作框，但拖选多日会直接打开右侧抽屉，且快捷操作框优先位于触发格左右、尺寸偏大并遮挡订单内容。U2 再次退回 `in_progress`：单击与拖选都必须先显示同一轻量操作框，选择具体动作后才进入覆盖式抽屉；桌面优先在所选行下方定位，下方不足时放到上方，宽度收紧且不覆盖触发格。本轮不修改业务动作可用性、订单、计价、库存、会员权益或清洁规则，正式 4.2 不开始。
- 2026-07-28：U2 人工验收返工完成并重新转为 `awaiting_user_acceptance`。点击新格会原子替换旧拖选区间；单笔订单浮层只高亮完整 Stay，父房多订单不猜测，选择准确订单后展开对应床位并保持旧区间消失；失效锚点、物业/账号切换、住宿从投影消失及抽屉重开均不恢复过期选区。最终门禁为 TypeScript、production build、`git diff --check`、Unit `410/410`、U2 桌面/手机完整 E2E `10 passed / 10 expected skipped / 0 failed`、阶段 7 选区与抽屉回归 `4/4`。双重只读审查完成；跨价格产品换房的金额可见性继续只归阶段 11，本轮未修改 `MOVE_UNIT`、计价、库存、会员权益或清洁规则。独立验收库不重置，实例 `http://127.0.0.1:4231/` 等待用户回复 `U2 通过`；正式 4.2 不开始。
- 2026-07-28：U2 人工验收发现拖选多日区间后点击其他普通格或准确订单时，旧选区仍与新上下文同时高亮；U2 退回 `in_progress`。本轮只修复选择状态互斥并补空格、单笔住宿格和父房多订单格真实浏览器回归；其他已通过项目继续有效。同期检查刚办理的一人现金订单，真实事实为 `109 · 四人间（公卫）` 整房换至 `104-A · 二人间床位 A`，服务端计价从 `¥232` 重算为 `¥68`，但工作人员反馈当前页面没有感知金额变化。跨房型不是一律禁止，价格产品不同时必须在阶段 11 原子重价并形成新 pricing revision，核对和完成后的当前页面都必须清楚显示原金额、新金额与差额；旧渠道价和旧人工调价不得自动继承。该问题只登记到阶段 11，本轮不修改 `MOVE_UNIT`、计价、库存、权益或命令可用性。
- 2026-07-28：U2 完成并转为 `awaiting_user_acceptance`。服务端从不可变订单、住宿、分段、计价和资金事实形成原始安排、当前/终态安排、履约结果和中文变更历史四层只读投影，损坏链继续失败关闭；工作人员订单页移除机器标识，房态使用就近快捷浮层、覆盖式查看/写抽屉、父房多订单列表、双轴/焦点恢复和 `176px/60px` 可持久导航。最终门禁为 TypeScript、production build、`git diff --check`、Unit `408/408`、Integration `183/183`、Contract/OpenAPI `58/58`、U2/阶段 7/阶段 8 联合桌面与手机 E2E `27 passed / 27 expected skipped / 0 failed`。独立数据库 `qintopia_u2_acceptance` 与实例 `http://127.0.0.1:4231/` 已启动；正式 4.2、阶段 9-14、Token 和清洁业务规则未开始。
- 2026-07-28：用户逐项确认 U1 的 5 项人工验收均为 `OK`，U1 转为 `accepted`，U2 转为 `in_progress`。本切片先提交页面机器信息清单和冻结实施规格，再收敛订单详情四层只读投影、房态快捷浮层、覆盖式抽屉、可折叠窄导航及渠道金额可见文案；不启动正式 4.2、阶段 9-13、Token 或清洁业务规则。
- 2026-07-27：U1 人工验收返修完成并重新转为 `awaiting_user_acceptance`。入住、退房只保留单层核对，“办理备注”为选填，空值生成稳定中文审计备注；桌面和手机均从房态订单上下文办理，不跳订单详情；调价默认当前订单金额且只接受非负整元。房态继续轮询以发现 revision、权限和投影变化，仅容忍已开始核对后的 5 秒自然 freshness 过期；未知结果恢复不会刷新无关订单。最终门禁为 Unit `330/330`、U1 E2E `3 passed / 3 expected skipped`、Stage 8 E2E `5 passed / 5 expected skipped`、调价与会员备注 E2E `2/2`、TypeScript、production build 与 `git diff --check`。U2、正式 4.2、阶段 9-13、Token 和清洁业务规则均未启动或修改。
- 2026-07-27：U1 人工验收退回 `in_progress`。入住/退房将去掉重复的“返回修改”层，“办理原因”改为可选备注，空值以系统审计备注满足既有后端合同；房态订单上下文的入住/退房改为当前页共享弹窗；调价当前金额已在真实页面正确默认，本轮只增加回归断言。不修改服务端业务规则，不启动 U2、正式 4.2 或阶段 9-13。
- 2026-07-27：U1 完成并转为 `awaiting_user_acceptance`。13 个白名单命令统一为自动服务端核对、一次中文确认、返回修改保留草稿、提交中阻断重复动作、结果未知只查原幂等键、成功自动关闭并刷新权威投影。评审进一步关闭 Escape/X/遮罩草稿丢失、迟到键覆盖、损坏 Receipt、刷新假成功、焦点丢失、恢复存储泄露业务 Receipt，以及调价/维修释放 effect 串线。TypeScript、production build、`git diff --check`、Unit `316/316`、Integration `182/182`、Contract/OpenAPI `57/57`、pricing facts `7/7`、U1 E2E `3 passed / 3 expected skipped`、Stage 8 独立 E2E `5 passed / 5 expected skipped` 均通过。完整 E2E 仍暴露 U1 排除范围的既有 Token 与房态性能/交互断言；两条 Stage 8 共享数据冲突经独立复跑全绿，一条违反 4.1 提前退房门禁的旧手机用例已收窄为合法入住并通过。U2、正式 4.2、阶段 9-13、Token 和清洁业务规则均未启动或修改。
- 2026-07-27：用户批准 [`QinTopia-PMS-第4步-U1-共享命令外壳-实施规格.md`](./QinTopia-PMS-第4步-U1-共享命令外壳-实施规格.md)，U1 从 `pending` 转为 `in_progress`。本切片只收敛 13 个已验收业务命令的自动服务端核对、一次确认、草稿返回、中文恢复和成功自动关闭；不修改 U2、正式检查点 4.2、阶段 9-13、Token 或清洁业务规则。
- 2026-07-27：用户明确回复“7R 通过”，阶段 7R 从 `awaiting_user_acceptance` 转为 `accepted`。换房前后双向 Stay 选择、完整订单返回后的最新位置恢复、同名订单隔离和分床定位符合预期；当前序列移至 `stage_u1_pending`，U2、正式检查点 4.2 及阶段 9 以后仍未开始。
- 2026-07-27：阶段 7R 完成并转为 `awaiting_user_acceptance`。真实浏览器验证从换房前或换房后分段进入均以稳定 `orderId/stayId` 打开同一订单、共同高亮有效分段；从完整详情执行换房返回后按最新房态恢复位置。路由身份、全分页事实一致性、父房/床位等价投影、多个 direct interval、`PARTIAL` 投影及损坏/跨物业状态均失败关闭。最终门禁为 Unit `27/27`、Integration `182/182`、Stage 7 桌面/手机 E2E `13 passed / 13 expected skipped / 0 failed`、TypeScript、production build 与 `git diff --check` 通过。U1/U2、正式检查点 4.2 和阶段 9 及以后保持未开始。
- 2026-07-27：用户确认现有范围讨论完成并同意继续，阶段 7R 从 `pending` 转为 `in_progress`。本轮只恢复可运行的 Stage 7 换房选择回归，并验证从原房源或新房源点击都以稳定 `orderId/stayId` 打开同一权威订单上下文、共同高亮可见窗口内全部有效分段；不改变 MOVE_UNIT、换房计价、库存、会员权益，不启动 U1/U2 或正式检查点 4.2。
- 2026-07-27：用户批准房态界面与阶段 14 纠偏并要求在对应阶段单独提醒验收。U2 新增房态格快捷浮层、覆盖式 `420-480px` 抽屉、桌面导航约 `176px/60px` 展开收起、单一房态视图及“本单渠道应结金额”统一文案门禁，必须等待 `U2 通过`。阶段 14 由旧 90 天草案改为任意日期起点的连续 30 天跨月时间轴、整页纵向滚动、楼栋/房间/可拆床位按需层级、日期头 `剩 X 间 · Y 床`、相邻住宿连续背景和最长 366 夜完整校验；阶段 14 开始前必须在保留现有未跟踪旧规格的前提下形成新版权威 companion。本次仅更新计划，不启动 7R、U1/U2、正式检查点 4.2 或阶段 14 产品实现。
- 2026-07-27：用户确认 104 游牧岛 `¥85`、106 携程 `¥84`、204 美团 `¥116`、D03 企业微信政策价、D04 企业微信主动偏价五组渠道计价人工测试均无问题，重新打开的阶段 2 从 `awaiting_user_acceptance` 转为 `accepted`。该切片以提交 `e1f583f` 和验收数据修复提交 `ac9ff60` 交付，测试基线为 TypeScript、Unit `300/300`、Integration `182/182`、Contract/OpenAPI `57/57`、阶段 2 桌面与手机 E2E `6/6`、pricing facts `7/7` 与 production build 全部通过。当前序列移至 `stage_7r_pending`；本次只记录验收结果，不启动阶段 7R、U1/U2、正式检查点 4.2 或阶段 9 及以后。
- 2026-07-27：阶段 2 首轮人工验收准备错误地把五笔 104 房样例排到当前 21 天房态查询范围之外，用户无法选择，阶段 2 验收被阻断。产品的 21 天临时边界保持不变，90 天连续时间轴仍归阶段 14；验收库改为在当前窗口内使用 104、106、204、D03、D04 五个不同房间的同一晚样例，直接点击房态格即可分别验证外部渠道 85%/84%/116% 和 WECOM 政策价/主动偏价。独立库保持零订单，修复后阶段 2 重新进入 `awaiting_user_acceptance`。
- 2026-07-26：阶段 2 渠道金额与 CREATE_ORDER 原子计价返工完成并转为 `awaiting_user_acceptance`。YOUMUDAO、CTRIP、MEITUAN 的渠道订单号与本单渠道应结金额必填；15% 整数边界、超界说明、WECOM 政策价/主动偏价、免费与会员既有计价类型及 PostgreSQL 同事务首个 pricing revision 均已端到端覆盖。TypeScript、Unit `300/300`、Integration `182/182`、Contract/OpenAPI `57/57`、阶段 2 桌面与手机 E2E `6/6`、production build 通过；验收实例 `http://127.0.0.1:4193/` 使用独立数据库且未预写业务订单。阶段 7R、U1/U2 和阶段 9 及以后保持未开始。
- 2026-07-26：4.1 验收状态、自动化结果与计划记录已通过独立提交 `1ab8872` 固定；按用户批准的纠偏例外重新打开阶段 2，状态转为 `in_progress`。本切片只实施渠道订单号/合同金额、15% 说明门槛、WECOM 直订偏价原因与 CREATE_ORDER 原子首个 revision；阶段 7R、U1/U2 和阶段 9 及以后保持未开始。
- 2026-07-26：用户明确回复“4.1 通过”，阶段 8 从 `awaiting_user_acceptance` 转为 `accepted`。本次通过只关闭 4.1/阶段 8，不代表第 4 步整体完成；后续按已批准纠偏顺序先重新打开阶段 2，并在其独立人工验收通过前不进入阶段 7R、U1/U2 或阶段 9。
- 2026-07-26：4.1 两项人工验收阻断缺陷修复完成，阶段 8 重新转为 `awaiting_user_acceptance`。未来入住、逾期入住和提前退房的中文门禁原因已归入订单操作区；逾期在住订单只保留非阻断“待迟录退房”入口，不再把当前或未来房态自动延期，迟录完成后任务消失。最终门禁为 Unit `290/290`、Integration `171/171`（房态聚焦 `21/21`）、Contract/OpenAPI `57/57`、Stage 8 E2E `5 passed / 5 expected skipped / 0 failures`、TypeScript、production build 与 `git diff --check` 全部通过。阶段 2 返工、阶段 7R、U1/U2 和阶段 9 及以后均未开始。
- 2026-07-26：4.1 人工验收发现未来/逾期入住及提前退房的中文原因虽已生成，但位于订单操作区之外，工作人员无法理解隐藏按钮；同时逾期在住订单被服务端合成到当前营业日，错误显示住客、“在住”和不可售。阶段 8 退回 `in_progress`。修复只把门禁原因归位，并保留非阻断待迟录退房任务、停止当前/未来房态自动延期；不进入退款、取消、未到、续住、缩短或正式 4.2。
- 2026-07-26：4.1 日期门禁、逾期迟录事实、提前退房旁路关闭、库存释放与停用清洁回归完成，阶段 8 转为 `awaiting_user_acceptance`。订单页对未来到店和逾期未入住分别给出严格中文失败关闭说明，Stage 8 增加“初晴/远帆”独立验收样例。最终门禁为 Unit `289/289`、Integration `171/171`（房态聚焦 `21/21`）、Contract/OpenAPI `57/57`、Stage 8 E2E `4 passed / 4 expected skipped / 0 failures`、TypeScript、production build 与 `git diff --check` 全部通过；首次完整 Integration 中一条无关命令协议压力测试因系统负载超过 4 秒，隔离复跑 `8/8` 和完整复跑 `171/171` 均通过。未开始阶段 2 返工、阶段 7R、U1/U2 或阶段 9 及以后。
- 2026-07-26：用户补充确认四条经营规则并覆盖同日更早的待决口径：（1）入住当天提前退房至少收一晚，允许操作员调价，归阶段 10；（2）计划到店日 20:00 后允许人工标记未到，也允许改期或补办入住，归阶段 9/12；（3）超过计划退房日仍在住时系统不区分真实超期，统一按原计划退房日迟录，4.1 立即实施，订单日期/金额/计价修订不变；（4）付费取消/未到默认全额退款参考并保留人工调整通道，状态处理归阶段 12、实际退款归阶段 13。4.1 同时关闭“在住订单先缩短到当天、再普通退房”的非原子提前退房旁路，并以 typed fulfillment DTO 分离生效日期、办理营业日和系统记录时间。
- 2026-07-26：根据 4.1 星河提前退房及“住宿分段”人工验收，完成状态乘营业日期审计。确认预订改期不删除/取消原订单，也不重新下单；阶段 9 扩为未入住订单调整入住日/退房日与在住续住，阶段 10 收窄为在住缩短和提前退房，并明确新退房日相对营业日的大于/等于/小于三分支。4.1 只开放计划到店日普通入住与计划退房日普通退房，提前/逾期履约在权威流程完成前失败关闭。入住当天退房最低收费、逾期未到时点及真实超期住宿/迟录退房区分保留为局部 Gate。订单详情未来按原始安排、当前/最后安排、履约结果、安排变更历史四层呈现；原“4.2A/4.2B”改名为前置纠偏 U1/U2，避免与正式检查点 4.2 冲突。本轮只更新计划，不开始产品实现。
- 2026-07-26：4.1 人工验收关闭提前退房开放决策：普通退房只处理计划退房营业日；计划退房日前的在住订单必须在阶段 10 使用原子的“缩短、原锁定政策重价、退款参考、库存释放、结束住宿”流程并填写真实原因，实际退款仍归阶段 13。计划退房日后的在住订单在“真实超期住宿/工作人员迟录退房”流程确认前同样失败关闭。阶段 10 完成前普通 `CHECK_OUT` 对提前退房失败关闭，阶段 8/4.1 退回 `in_progress`；验收样例不得再用提前退房冒充普通退房。
- 2026-07-26：用户批准 [`sprint-change-proposal-2026-07-26-channel-order-pricing-and-4.2.md`](./sprint-change-proposal-2026-07-26-channel-order-pricing-and-4.2.md)。外部渠道订单的合同金额在页面命名为“本单渠道应结金额”，渠道订单号与该金额均必填；与政策基础金额绝对差异率 15% 及以内无需说明，超过 15% 必填“渠道价格差异说明”。WECOM 直订偏离政策价和创建后价格更正始终要求原因。目标合同金额必须随 CREATE_ORDER 原子形成首个 revision，禁止建单后再改价。PMS 不建设渠道打款、银行流水或财务对账分摊；外部智能体通过 API 取得业务事实后在系统外对总账。实施顺序调整为 4.1 人工通过后先返工阶段 2，再处理阶段 7 选择回归和前置纠偏 U1/U2。
- 2026-07-26：按已批准的 [`spec-disable-cleaning-workflow.md`](../docs/implementation/spec-disable-cleaning-workflow.md) 暂停当前版本清洁工作流。阶段 8/检查点 4.1 收窄为入住、退房、会员权益核销与库存释放；退房不生成清洁事实，房态与订单不投影既有 `PENDING` 记录，完成清洁命令失败关闭。清洁 schema、历史记录、命令契约、Receipt 和审计保留；未来重新启用必须重新确认日期语义、逾期呈现、可售关系、权限及历史 `PENDING` 激活/截止政策。阶段 9 及以后状态不变。
- 2026-07-23：建立分步开发计划；确认免费入住必须复用普通住宿同一套主要居住人信息、字段和校验；确认每个阶段由用户在真实 UI 上人工验收通过后，才进入下一阶段。
- 2026-07-23：完成独立一致性终审；同步正常/免费渠道权威边界、工作人员中文协议文案、结构化操作意图 reason、`memberId` 会员选择、续缩换本次调价及会员跨权益类型换房失败关闭。阶段 4 的多 Lot 自动 coverage 前仍需确认消耗顺序，该 gate 不阻断阶段 1 至 3 及阶段 4 的会员建档/会员订单部分。
- 2026-07-23：根据房态截图新增阶段 5，要求多人间父房逐日格直接显示每一个有权住客昵称及占用比例，禁止“首个昵称 +N”或仅悬停补全；原阶段 5 至 14 顺延为 6 至 15，90 天时间轴改为阶段 14。
- 2026-07-23：根据 `101A` 王羽连续住宿截图重写阶段 7；点击已占日期或昵称必须按稳定 Order/Stay 引用选择完整连续住宿，续住与无缝换房分段保持同一选择，右侧从报价草稿切换为权威订单概述和服务端允许动作。
- 2026-07-23：确认“1 个阻断”只是正常库存占用的机器事实，不是工作人员异常；阶段 5/7 移除格子、Tooltip、屏读、移动端和订单上下文中的阻断/Claim 技术词，阶段 8 统一住宿生命周期为“已预订 → 在住 → 已退房”，清洁任务另行显示“待清洁/已完成”；待清洁是否影响可售保持局部待决，底层冲突与并发互斥合同不变。
- 2026-07-23：新增面向用户的唯一人工验收入口并重排阶段 3/4，使免费入住归入首次开单、会员归入独立闭环；会员阶段扩展为统一会员列表、身份证唯一档案、三种有限会员订单、多笔企微收款、显式生效、账本余额和按需会员入住。
- 2026-07-23：确认会员订单默认价可改且需原因，至少一笔企微收款后由工作人员显式生效，差额只提示，有效期从生效起算一年；当前版本在合同拟定前不处理会员退费。资金与权益更正均追加事实，不覆盖不可变历史。
- 2026-07-23：加入“住宿收款转入会员订单”的受限原子旅程：同住客、匹配房型、只转完整原 COLLECTION、防重复、新差额收款使用新企微交易号，并以 `¥590 + ¥1,030 -> 30 夜 - 核销 7 夜 = 剩余 23 夜` 作为金标。
- 2026-07-23：D/E 楼 PMS 生成房号待统一为 `D01-D05`、`E01-E03`；同步纳入人民币元录入、逾期未到可见性和损坏恢复记录处置等发布前体验门禁。内部占用保持局部待决，不阻断已确认核心旅程。
- 2026-07-23：阶段 1 拖选改为网格内本地预览、松键后一次性提交；新增逐帧右侧滚动几何和 Quote 请求数量门禁，修复验收发现的右侧滚动条上下抖动，重新进入 `awaiting_user_acceptance`。
- 2026-07-23：阶段 1 统一房态中所有房源的楼栋 / 房号 / 房型展示，修复 `302 · 302` 等重复拼接，新增 302 真实浏览器文案门禁。
- 2026-07-24：人工第 2 步的 2A、2B 已通过；2C 完成账本余额、目标余额更正、按需会员住宿、精确房型匹配、部分覆盖及 HOLD/CONSUME/RELEASE 自动化验证，阶段 4 转为 `awaiting_user_acceptance`，未进入阶段 5。
- 2026-07-24：2C 人工验收发现 CONSUME 行误展示内部余额净变化 `0`；已改为显示该住宿日期实际核销 `1 间夜/床夜`，底层账本守恒不变，阶段 4 重新进入 `awaiting_user_acceptance`。
- 2026-07-24：2C 复验进一步明确数量语义：冻结/释放/更正/到期统一显示“余额 ±N”，入住显示“本次核销 N”，不以 `-N` 误导为入住时再次扣减。
- 2026-07-24：2C 复验确认多种正式会员权益为并行关系；正常会员页面不再显示无明确产品归属的“历史未归类权益”，底层旧账审计兼容能力保留，阶段 4 重新进入 `awaiting_user_acceptance`。
- 2026-07-24：2C 复验发现 Demo 已生效订单没有有效收款；已为种子和现有验收库追加 `¥1,620` 企微收款事实并补回归门禁，零收款不得生效的正常业务规则不变，阶段 4 重新进入 `awaiting_user_acceptance`。
- 2026-07-24：2C 复验确认会员住宿不应填写订单来源渠道，主按钮也不得显示 `Preview`。已将全额、部分和零余额会员住宿统一为渠道为空，加入服务端、Confirm 与迁移 019 数据库守卫；核对、成功、恢复和订单详情改为中文业务语义，阶段 4 重新进入 `awaiting_user_acceptance`。
- 2026-07-24：2C 复验要求选择会员后以会员姓名同时预填昵称和姓名，以会员手机号、身份证号预填联系电话和证件号码；这些字段仍是本次住宿独立快照，操作人可逐项修改。阶段 4 回到 `in_progress` 补充实现和浏览器门禁。
- 2026-07-24：会员资料预填与可编辑快照已完成；桌面、手机 E2E 均验证人工修改后 CREATE_ORDER 保存修改值，阶段 4 重新进入 `awaiting_user_acceptance`。
- 2026-07-24：记录第 3 步房态展示决定：日历日期格和跨日住宿横条不显示订单号，只显示居住人昵称与必要状态；订单号仅在右侧订单上下文和完整详情显示，底层稳定关联、点击定位和整段选择不变。本决定只纳入阶段 5/7 计划，未在第 2 步提前实施。
- 2026-07-24：用户明确回复“第 2 步通过”；阶段 4 状态转为 `accepted`。最终门禁为 Unit `222/222`、第 2 步 PostgreSQL 聚焦测试 `27/27`、Contract `56/56`、第 2 步桌面/手机 E2E `4/4`、TypeScript、production build 和 `git diff --check` 通过；完整 integration `154/155` 的唯一失败为第 1 步既有入住后缩短用例。随后按已批准范围进入第 3 步阶段 5。
- 2026-07-24：第 3 步阶段 5 完成整房多人住宿人、独立入住容量、多人间昵称横排、精简维修文案和隐私房态投影；Unit `244/244`、Integration `163/163`、Contract `56/56`、E2E `53 passed / 39 skipped`、Pricing facts `7/7`、TypeScript、production build 与 `git diff --check` 全部通过，状态转为 `awaiting_user_acceptance`。订单入口与逐位资料人工更正仍按确认方案留在阶段 7，阶段 6、7 未开始。
- 2026-07-24：阶段 5 人工验收进一步确认住宿展示：未点击时不显示重复的住宿文字横条，只显示逐日昵称、人数/比例和状态；点击后由阶段 7 使用无重复文字的整段连续高亮，并把订单入口统一放在右侧上下文。阶段 5 已移除整房与已聚合分床住宿横条，维修/锁房等 Block 横条保持不变。
- 2026-07-24：用户完成阶段 5 复验并回复“检查完了，继续”，阶段 5 转为 `accepted`，阶段 6 转为 `in_progress`，阶段 7 仍为 `pending`。
- 2026-07-25：用户免除阶段 6 单独人工检测并要求直接完成阶段 7；阶段 6 与阶段 7 均进入 `awaiting_user_acceptance`，合并桌面与手机端一次验收。内部占用继续保持 `deferred_decision`，阶段 8 未开始。
- 2026-07-25：阶段 7 完成稳定 Order/Stay 整段选择、整房与逐床准确订单入口、权威订单上下文、追加式住宿人资料更正、READ/WRITE 权限、外部刷新、详情返回恢复及响应式抽屉。最终门禁为 TypeScript 通过、Unit `262/262`、Integration `169/169`、Contract `57/57`、Pricing facts `7/7`、E2E `65 passed / 51 configured skips / 0 failures`、production build 与 `git diff --check` 通过。
- 2026-07-25：阶段 7 最终可视复查修复非模态桌面抽屉落在主表下方和 320px 原生滚动条导致的横向溢出；新增视口贴合、手机滚动锁定断言，修复后 desktop 聚焦 E2E `8/8`、mobile 聚焦 E2E `3/3` 通过，1440x900、375x812、320x700 实机浏览器复查无重叠或横向滚动。
- 2026-07-25：阶段 7 人工验收夹具从当天平移到未来 7 天，避免当天未入住订单触发逾期保护而使写入口按规则关闭；独立验收库已核对房态 `projectionState=READY` 且住宿人更正动作 enabled。
- 2026-07-25：阶段 7 人工验收发现固定订单抽屉在房态 4 秒轮询时短暂卸载上下文，造成闪动和滚动回跳；已让同一订单、同一权限范围的后台刷新保留当前授权视图与 DOM，并新增刷新前后 `.modal-body.scrollTop` 恒等 E2E。聚焦 desktop E2E 1/1、Unit 18/18、TypeScript 与 production build 通过；本机跨三个轮询周期复查无加载闪烁或回跳，阶段 7 保持 `awaiting_user_acceptance`。
- 2026-07-25：阶段 7 人工验收发现分床父房展开后具体床位格未渲染住宿人昵称；已将整房与具体床位的直接住宿统一按各自稳定区间显示昵称、状态和人数，父房聚合及逐床订单边界不变。五个住宿日的 A/B 床昵称隔离 E2E 1/1、相关 Unit 15/15、TypeScript、production build 与本机可视复查通过，阶段 7 保持 `awaiting_user_acceptance`。
- 2026-07-25：阶段 7 人工验收进一步要求不同分辨率下保持单日格可读且点击订单不放大；日期列固定为 94px，自动窗口按主表实际宽度适配 7 至 21 天，新增 `自动 / 7 / 14 / 21` 控件，并让普通与订单上下文均可收起重开。1280/1366/1440、1920 与手机聚焦 E2E 12/12 通过，阶段 8 未开始。
- 2026-07-25：人工验收发现当前查询只有 14 夜时选择 `21` 仍只能显示 14 天；已将新会话默认查询补足为 21 夜，并让显式选择更长档位时同步补足结束日期。该修复只收口阶段 7 日期窗口合同，不进入 90 天阶段。
- 2026-07-25：用户明确回复“第三步通过”；阶段 6 与阶段 7 随第 3 步合并验收转为 `accepted`，内部占用继续保持 `deferred_decision`，阶段 8 未开始。最终门禁为 TypeScript、Unit `264/264`、Integration `169/169`、Contract `57/57`、Pricing facts `7/7`、阶段 7 桌面/手机 E2E `12/12`、production build 与 `git diff --check` 全部通过。
- 2026-07-25：第 4 步检查点 4.1（阶段 8）完成普通、会员、免费住宿的入住/退房业务呈现、会员 HELD coverage 一次性核销、独立待清洁/已完成任务、履约结果恢复和动作后返回房态；待清洁是否阻断可售继续保持待决，未进入续住及后续阶段。最终门禁为 Unit `268/268`、Integration `169/169`、Contract `57/57`、TypeScript、production build、`git diff --check` 及阶段 8 桌面/手机 E2E `4 passed / 4 project-matrix skipped / 0 failures` 全部通过，阶段 8 转为 `awaiting_user_acceptance`。
- 2026-07-25：4.1 人工验收发现带 `?action=CHECK_OUT` 的订单详情在 4 秒后台轮询后重复定位退房按钮，导致页面滚动回跳；已让动作定位只在动作参数首次变为可用时执行，并新增等待真实订单详情轮询及两个绘制帧后 `window.scrollY` 不变的 E2E。聚焦 Unit `20/20`、目标桌面 E2E `1/1`、阶段 8 桌面 E2E `3 passed / 1 expected skip`、TypeScript、production build 与 `git diff --check` 通过，阶段 8 保持 `awaiting_user_acceptance`，阶段 9 未开始。
