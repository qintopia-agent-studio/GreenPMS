# AGENTS.md instructions

<INSTRUCTIONS>
在条件允许的前提下，我批准你使用多个 sub-agent 来完成任务。

## 权威来源与需求边界

- 人工验收步骤和完成状态以 `待开发项/QinTopia-PMS-分步开发与人工验收计划.md` 为准；字段、接口、事务、并发和测试规则以对应实施规格及工程实施清单为准。不同来源冲突时不得自行选择，先核对最新已确认决定；仍无法消除且会实质改变结果时，向用户提出聚焦问题。
- 开始实现前，必须明确目标、约束和最低验收标准。先从用户已确认决定、规格、代码、测试和历史记录中查证；只有答案无法发现且不同选择会实质改变资金、权益、生命周期、权限、架构或验收结果时，才向用户提问。简单且可逆的实现细节遵循现有模式直接处理。
- 权威业务规则优先来自用户已确认决定和对应实施规格，不得仅根据当前代码反推或新增业务语义。
- 使用外部 API 前查阅官方文档并验证关键行为；使用内部 API 前核对契约、源码、测试和运行证据。文档与实际证据冲突时停止依赖并报告。

## 风险分级与验收

开发切片按风险分级，不对所有切片使用同一套流程：

- **A 级（完整 A 级流程）**：涉及计价、资金、库存增减、事务与并发、订单生命周期状态迁移、权限与安全恢复的改动。必须依次完成 实施规格 → 开发 → 系统自动检查 → 逐步人工验收。
- **高风险任务**：包括所有 A 级任务、核心业务不变量重新判断，以及最终安全、契约和高风险边界审查。
- **B 级（轻流程）**：仅限纯 UI/UX、文案、布局和交互手感类切片。目标和最低验收标准明确后，不单独编写实施规格和验收计划；直接修改页面并提供可访问网址或截图，允许根据反馈迭代视觉细节。用户满意后，在 `待开发项/deferred-work.md` 或对应步骤段落记录变更即视为通过。
- 即使只修改前端，只要影响写入门禁、权限、资金表达、库存判断、订单生命周期、恢复流程或安全状态，都不得按 B 级处理。
- A 级允许将 2-3 个相互独立的小检查点合并为一次人工验收；验收指引必须逐一列出操作步骤，任一不通过只返工对应检查点。
- 分级有争议时就高不就低，拿不准一律按 A 级处理。

## Sub-agent 调度与模型路由

### 调度能力和参数

- 根代理始终使用当前父代理配置的模型，不主动切换。以下模型路由规则只约束 sub-agent。
- 是否能够派发 sub-agent 取决于当前 Codex 环境是否暴露 `spawn_agent` 工具，以及用户或本文件是否授权派发，不取决于模型名称本身。
- 模型是否可用以及参数如何组合，以当前 `spawn_agent` 工具声明为准，不得根据名称猜测。
- `worker`、`explorer`、`awaiter` 只是任务分类，不是 `spawn_agent` 参数。
- 显式指定 `model` 或 `reasoning_effort` 时，必须将 `fork_turns` 设置为 `none` 或正整数；使用 `fork_turns: all` 继承完整上下文时，必须省略模型和推理档位覆盖。
- 派发时应显式设置适合任务的模型和推理档位；只有任务确实需要继承根代理的完整上下文和配置时，才省略覆盖值。

### 按任务选择模型

- 重新判断资金事务、并发、幂等、数据库边界或其他核心业务不变量：`gpt-5.6-sol` + `ultra`。
- 已有明确、冻结实施规格的核心代码实现，即使跨模块：`gpt-5.5` + `xhigh`。界面的 `Extra High` 对应传入值仍为 `xhigh`。
- 实现过程中需要修改原规格，或自行决定核心业务不变量：`gpt-5.6-sol` + `ultra`。
- 最终安全、契约、回归和高风险边界审查：`gpt-5.6-sol` + `ultra`。
- 局部测试补充、普通修复、UI 调整和机械性工作：`gpt-5.6-terra` 或 `gpt-5.6-luna`，推理档位按任务复杂度选择。
- 只读代码探索：默认 `gpt-5.6-luna` + `low`；涉及跨模块架构判断时使用 `gpt-5.6-sol` + `ultra`。
- 等待或轮询任务状态：`gpt-5.6-luna` + `low`。
- 高风险任务所规定的模型无法调用时，整个高风险任务必须暂停；根代理只能继续只读排查和报告，不得实现、批准或提交该任务。
- 非高风险任务的指定模型调用失败时，优先改用可用的 `gpt-5.6-terra`，并在最终汇报中说明模型切换；如果 Terra 也不可用，停止猜测模型 ID 并报告阻塞原因。

### 委派与并行

- 先判断当前任务的直接阻塞点；不要把下一步立即依赖的工作委派后原地等待。
- 委派任务必须具体、独立、可验收，并且确实推进主任务；不得把同一未解决问题重复委派给多个 sub-agent。
- 多个 worker 并行前必须完成 ownership 检查，为每个 worker 分配互不重叠的文件或模块范围。
- 每个并行 worker 的任务描述必须说明代码库中有其他 agent 同时工作，不得回滚或覆盖他人的改动，发生冲突时适应已有改动。
- explorer 和 awaiter 只读，可并行；worker 只有在修改范围互不重叠时才可并行。
- 委派的代码任务必须要求 sub-agent 直接修改共享工作区，并在完成时列出修改过的文件。
- 委派完成后不要重复实现同一任务；根代理负责审查、整合和处理不重叠的工作。
- 所有并行 worker 完成后，根代理必须审查合并后的整体差异；涉及代码时，还必须运行覆盖整体风险的构建和相关测试，不能只采信各 worker 的局部结果。

## 工程与验证原则

- 不新增语义重复的实现；优先复用现有代码、组件、工具和资源，但表面相似、变化原因不同的代码不得强行统一抽象。
- 遵循既有架构、标准和模式；重大架构变更必须先沟通确认。
- 产生可观察行为变化时，必须编写或更新相关测试；文档、注释和纯机械修改执行适用的格式、链接或静态检查。
- 不跳过验证，也不以测试数量代替证据。运行与变更风险匹配的最小验证集，必要时再扩展；交付前，相关测试及质量门禁必须全部通过。
- 保持改动小范围、可审查，并在有意义的风险边界逐步验证，不进行宽泛或冒险的修改。
</INSTRUCTIONS>
