# Green PMS 项目约定

- 修改通过 PR 合入 main；标题、正文与合并保护见 `docs/repository-contributing.md`，模板为 `.github/PULL_REQUEST_TEMPLATE.md`。
- PR 格式本地验证：`node --test scripts/check-pr-tests.mjs`。保护配置记录在 `.github/main-protection.json`；不要通过降低保护要求绕过失败检查或待审批。

## CI 流程保护（用户于 2026-09-19 明确要求）

- 用户指定 `.github/workflows/ci.yml` 保持 Evans 的 `486b4bf8f7446a1a7336542d0e40e030cbaa5f00` 版本，作为明确基准；不得自行新增 CI job、步骤或改变原有行为。该基准只针对 `ci.yml`，不得把其他发布／回退工作流一并回退到该提交。

- 以后不得擅自修改原同事编写的 CI 流程，包括工作流触发条件、检查任务、执行顺序、权限和调用入口；在原工作流中新增 job 也属于修改流程。
- 普通功能修复、补充回归测试、处理 CI 失败，都不构成修改 CI 流程的授权。不得通过修改入口脚本间接改变原流程的执行范围，也不得跳过、删除或放宽原有检查来让 CI 通过。
- 如确需调整，先说明具体差异和影响，只有用户后续明确授权该项调整后才可实施；其他已授权工作继续推进。

## Worktree 使用与收尾

- 默认复用项目根目录或已有的合适工作目录，在功能分支上开发；新任务、创建分支或提交 PR 本身不构成新建 worktree 的理由。
- 只有用户明确要求隔离、并行任务需要独立文件状态、现有未提交工作阻碍本次安全操作，或必须同时运行不同版本时才新建。先检查 `git worktree list`、分支和未提交状态，优先复用；无关的未提交文件不自动构成隔离理由。
- 创建前说明具体隔离原因、路径、分支和收尾条件；在已授权任务内自主执行，不新增逐步审批。手工创建的 worktree 统一放在目标仓库已忽略的 `.worktrees/<任务名>/`，不在 `/tmp` 或 `/private/tmp` 长期开发；用户指定或应用托管的目录遵循其已有管理方式。
- 交付时核对 PR 实际合并状态（包括 squash merge）、未提交和未跟踪文件、被忽略的配置与验收资料，以及运行中的预览。仅清理本任务已完成且可恢复的目录；有待验收工作则保留并说明用途。资料归档到已忽略的 `.local-workspace/`，不得把凭据或业务快照随代码提交。
- 分支合并、任务归档和 worktree 清理是独立动作，不能把其中之一当作其他动作已完成。整理主目录时先保存所有工作，在不打断其他任务的前提下恢复到最新 `main`；不强制覆盖或一并删除历史分支。
- 跨仓库操作先定位目标 Git 根目录并读取其 `AGENTS.md`。Codex 中的项目分组不改变磁盘目录层级或 Git 仓库边界；从 QinTopia Agent OS 任务操作本仓库时同样遵循本节。

## 发布 harness 索引

- 日常发布、回退与首次配置：`docs/operations/production-release-quickstart.md`；实现细节与恢复：`docs/operations/production-release-runbook.md`；参考方案映射：`docs/operations/production-release-investigation.md`。
- `main` 合并后由 `.github/workflows/release-please.yml` 自动生成版本 PR、tag 和 Draft Release；正式部署仍只响应 `release.published`。
- 遵循 npm workspaces / package-lock：`npm ci`、`npm run release:check`、`npm run typecheck`、`npm test`、`npm run build`；离线发布测试：`npm run test:release`（Python 3，fake COS/Docker/SSH）。
- 生产 `compose.server.yaml` 只接收预构建 `GREENPMS_IMAGE`；不要在生产目录构建，不读凭据文件，不把外部 TencentDB/备份列入清理。部署与清理使用 `scripts/release/` 正式入口。

本文件补充全局 `AGENTS.md`，定义本项目的业务来源、风险分级和验收要求。

## 业务来源

- 实现前从已确认决定和项目资料明确目标、约束与最低验收标准；已有规格足够时直接复用，不为同一切片重写文档或重复请求确认。
- 人工验收步骤和状态以 `待开发项/QinTopia-PMS-分步开发与人工验收计划.md` 为准；字段、接口、事务、并发和测试规则以对应实施规格及工程实施清单为准。最新明确的用户决定可以更新旧规则，需同步对应文档。
- 代码和测试说明当前实现，不单独构成新增业务语义的依据。来源冲突时先核对最新已确认决定；仍无法消除且影响业务或验收结果时，只询问冲突点，继续不依赖该决定的工作。
- 外部 API 在首次使用、契约变化或关键行为不确定时核对官方文档；内部 API 核对相关契约、源码、测试及必要的运行证据。已验证且未变化的契约不重复调查；文档与实际行为冲突时暂停依赖该行为的动作，查明后继续。

## 风险与验收

按实际行为和影响分级，不按文件位置或任务名称分级：

- **A 级**：计价、资金、库存增减、事务与并发、订单生命周期迁移、权限与安全恢复，以及这些业务不变量的判断。流程为适用实施规格 → 开发 → 自动检查 → 人工验收。未决业务选择先解决；已明确的规格不新增“批准开发”门槛。
- **B 级**：纯 UI/UX、文案、布局和交互手感。目标与最低标准明确后直接修改，提供可访问网址或截图，不另建实施规格和验收计划。影响写入门禁、资金含义、库存判断、生命周期、权限或恢复的前端改动按 A 级处理。
- **其他工程改动**：文档、测试维护、机械修改及不影响 A 级行为的普通修复，直接实施并做相关检查。触及 A 级边界则升级对应部分；不因不属于 B 级就自动要求完整 A 级流程。
- 风险暂不明确时先做针对性只读调查；关键影响仍无法排除时，该部分按 A 级处理。高风险要求更强证据和验证，不自动要求每一步请示或指定某个子模型。
- 人工验收在实现和自动检查完成后提交，可将 2-3 个独立小检查点合并一次验收，逐项列操作与预期结果；失败返工对应部分，并复查实际受影响的依赖。
- 分开记录工程完成与人工验收。等待验收时交付已完成结果和准确状态，继续其他不依赖验收的已授权工作；不能把待验收标成通过，或跨越计划明确要求先通过的依赖门槛。
- B 级在用户明确满意后，更新 `待开发项/deferred-work.md` 或对应步骤段落为通过；反馈前记录“已实现，待反馈”，记录本身无需另行批准。

## 项目验证

- 遵循既有架构、组件和业务边界。重大架构调整涉及尚未授权的范围或会改变已确认约束时先沟通；已经明确要求的架构调整按授权推进。
- 业务行为变化须有相关回归证据；优先运行有效的现有测试，缺少覆盖或语义改变时补充或更新测试。A 级关键不变量、事务、并发及权限边界按相应规格验证。
- 纯视觉改动检查页面交互和截图；文档与机械修改做适用的格式、链接或静态检查。相关检查通过且无新疑点即交付；环境阻塞、已有失败和本次引入的问题如实区分，不伪报验证或人工验收通过。
